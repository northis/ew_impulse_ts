# DIAGONAL — Диагональ (Leading / Ending Diagonal) как торговый сетап

> Проектный документ. Фиксирует видение отдельного детектора **сжимающихся
> диагоналей** (`DIAGONAL_CONTRACTING_INITIAL` / `DIAGONAL_CONTRACTING_ENDING`)
> для торговли разворота/коррекции, которые всегда следуют за диагональю.
>
> Статус: **дизайн, до реализации.** Реализацию начинаем после ревью этого
> документа. Аналог по подходу — [EW_R_TRIANGLE.md](EW_R_TRIANGLE.md)
> (running-треугольник): там же зафиксированы уроки, которые здесь
> переиспользуются (лестница периодов, cross-scale сборка, диагностика гейтов).

---

## 1. Мотивация и суть

Диагональ — терминальная модель: после неё **всегда** возникает движение в
противоположную сторону:

- **начальная (leading) диагональ** — волна 1 или A → за ней идёт **глубокая
  коррекция** (волна 2 или B), обычно 50–95% диагонали;
- **конечная (ending) диагональ** — волна 5 или C → за ней идёт **новый тренд**
  (разворот всей структуры).

Оба случая торгуются одинаково — **против направления диагонали**, поэтому
детектору не нужно знать, какая именно это диагональ (решение O-5, §11).

Ключевые свойства сетапа:

1. **Сигнал до завершения модели.** Мы не ждём подтверждения конца волны 5 —
   входим на закрытии свечи, **пробившей конец волны 3** после сформированной
   волны 4. Пробой волны 3 гарантирует, что усечения (truncation) исключены:
   волна 5 уже сделала новый экстремум.
2. **Стоп самомасштабируемый и точный**: в сжимающейся диагонали `|W5| < |W3|`
   (hard-правило), значит цена не может уйти дальше, чем `V(4) + длина W3`.
   Стоп ставится ровно на теоретический максимум волны 5 — его пробой
   *одновременно* и стоп, и инвалидация модели.
3. **TP через множитель R:R** от стопа (по умолчанию 1.0) — потому что цель
   после диагонали разная (коррекция vs новый тренд) и фиксированного
   структурного уровня, как у треугольника (точка 0 / волна B), здесь нет.

Расширяющиеся (expanding) диагонали **не рассматриваются** (решение O-1):
они крайне редки (коэффициенты 0.005–0.01 в [EW_RULES.md](EW_RULES.md) §8),
и для них не работает логика стопа от длины волны 3 (`|W5| > |W3|`).

---

## 2. Соотнесение с существующим кодом

| Аспект | Что уже есть | Что делаем в `DiagonalSetupFinder` |
|---|---|---|
| Правила диагонали | [`CheckDiagonalContracting`](TradeKit.Core/AlgoBase/ElliottWaveExactMarkupV2.PriceRules.cs) (v2): W2 не за начало W1, пробой W3 за W1 (`MIN_DIAGONAL_PENETRATION` = 5% |W1|), сжатие `|W3|<|W1|`, `|W4|<|W2|`, `|W5|<|W3|` | Явные инкрементальные правила §4 (без зависимости от общего markup). Отличие: **добавляем hard-правило перекрытия W4↔W1** (в v2 его нет) и **запрещаем усечение** (в v2 усечение разрешено для ENDING) |
| Зигзаг | [`DeviationExtremumFinder`](TradeKit.Core/Indicators/DeviationExtremumFinder.cs) — неизменяемое прошлое, основа инкрементальности | Тот же |
| Автопериод | [`AutoPeriodEstimator`](TradeKit.Core/AlgoBase/AutoPeriodEstimator.cs) (`Period == 0` → авто) | Тот же; базой берём импульсную оценку (диагональ — моторная модель), см. §7 |
| Мультимасштаб | Лестница `LADDER_RATIOS` из `RunningTriangleSetupFinder` (шаг ∜φ…φ) | Та же идея; дубли схлопываются по точке 0 + TP/SL-подписи |
| Cross-scale | `TryCrossScale` в `RunningTriangleSetupFinder`: старшие волны с крупного рунга, младшие — по **сырым барам** | Обязателен: триггер (пробой W3 закрытой свечой) — это событие **бара**, а не пивота зигзага, поэтому волна 5 всегда ведётся по сырым барам (§6.2) |
| «Заходность» | [`IsInitialMovement`](TradeKit.Core/Common/SingleSetupFinder.cs) | **Опционально, по умолчанию выключено** (решение O-6, §5.2): диагональ бывает и начальной, и конечной, поэтому требовать «свежести» точки 0 по умолчанию нельзя — но флаг `RequireInitialDiagonal` даёт включить гейт как контекстный фильтр и сравнить статистику. Проверяется **вся** диагональ `V(0) → W5`, включая бар сигнала |
| Диагностика | `Diag`/`Bump`/`OnGate` из `RunningTriangleSetupFinder` | Переносим (funnel-диагностика гейтов, §9) |
| Сигнал/события | `ElliottWaveSignalEventArgs`, `OnEnterInvoke`/`OnTakeProfitInvoke`/`OnStopLossInvoke`, `PERCENT_ALLOWANCE_TP/SL`, округление до `Symbol.Digits` | Как в `ImpulseSetupFinder`/`RunningTriangleSetupFinder` |
| Изолированные принты | Разрывы учитываются в `MovementStatistic`/`SmoothImpulseClassifier`, но бар-фильтра нет | `IsolatedPrintFilter` (§4.4): сегменты подтверждаются с задержкой и исключаются из пивотов, волны 5, D-CONTAIN и D-INSIDE |

**Урок из running-треугольника, критичный здесь** (EW_R_TRIANGLE.md §7.6):
список пивотов `DeviationExtremumFinder` зависит от истории воспроизведения —
мелкие внутриволновые экстремумы поглощаются `MoveExtremum` и транзиентно
исчезают. Поэтому пивотам доверяем только «скелет» (волны 1–4 на своём рунге),
а всё, что происходит **внутри текущей волны 5** (пробой V(3), длина 78–100%),
меряем по сырым барам — они детерминированы.

---

## 3. Геометрия и обозначения

Точки (по возрастанию времени): **0, 1, 2, 3, 4, 5** — концы волн. Волна `Wk` —
движение от точки `k−1` к точке `k`; каждая волна может состоять из **одного или
нескольких сегментов зигзага** (подволн). `V(x)` — цена точки x.

Пример для **бычьей диагонали** (`isUp` = диагональ вверх → сигнал **SELL**;
медвежий случай зеркален):

```
                                          ┄┄┄┄┄┄┄ SL = V(4) + |W3| (теор. максимум W5)
                                     ╱ (5)
                              3     ╱   ← сигнал: закрылась свеча, пробившая V(3)
                             ╱╲    ╱      вход SELL по close
                        1   ╱  ╲  ╱
                       ╱╲  ╱    ╲╱ 4  ← конец W4 в зоне W1 (перекрытие, hard)
                      ╱  ╲╱ 2
                     ╱   (W2 не ниже точки 0)
                    0                     ┄┄┄┄┄┄┄ TP = entry − R·(SL − entry)
```

- **0** — начало волны 1 (и всей диагонали); для `isUp` — low.
- **1, 3, 5** — моторные волны по направлению диагонали; **2, 4** — коррекции.
- Клин **сжимается**: `|W3| < |W1|`, `|W4| < |W2|`, `|W5| < |W3|`.
- **Перекрытие**: конец W4 заходит в ценовую зону W1 (ниже V(1) для `isUp`) —
  главный признак, отличающий диагональ от импульса.

Соглашение о направлении: `isUp` описывает **диагональ**. Сделка всегда
противоположна: бычья диагональ → SELL (TP ниже входа), медвежья → BUY.

---

## 4. Инварианты диагонали (hard rules)

Для `isUp` (медвежий случай — зеркально). Все проверки через знак
`s = isUp ? 1 : −1`, один код на оба направления.

- **D-W2** — `s·(V(2) − V(0)) > 0`: волна 2 не уходит за начало волны 1.
- **D-W2-RET** *(опционально, параметр `MinWave2Retrace`, по умолчанию **0**)* —
  `|W2| ≥ MinWave2Retrace·|W1|`: волна 2 откатывает заданную долю волны 1
  (значение в `[0, 1]`; `0` — ограничения нет, поведение прежнее). Парное
  к D-W4-38 правило для первой коррекции: клин, у которого волна 2 едва
  царапает волну 1, — это тренд, а не сжимающаяся диагональ; кроме того,
  мелкая волна 2 — частый симптом «перелитой» волны 1 (§9.11).
  Гейт — `w2RetraceTooShallow`, проверяется сразу после D-W2.
- **D-W3-PEN** — `s·(V(3) − V(1)) ≥ MinWave3Penetration·|W1|`: волна 3
  делает новый экстремум за волной 1. Порог — **параметр**, по умолчанию
  **0.03**: у плотного сжимающегося клина волна 3 пробивает волну 1 совсем
  чуть-чуть, и «как в v2» 5% отбрасывало валидные модели (§9.8).
- **D-CONTRACT-3** — `|W3| < |W1|` (сжатие).
- **D-CONTRACT-4** — `|W4| < |W2|` (сжатие).
- **D-W4-78** *(опционально, флаг `RequireWave4Ratio`)* — `|W4| ≥ 0.786·|W2|`:
  вместе с D-CONTRACT-4 даёт коридор `0.786·|W2| ≤ |W4| < |W2|` — клин
  сжимается равномерно, а не схлопывается крошечной волной 4. По
  умолчанию **выключено**: на архиве фильтр режет выборку втрое и
  ухудшает матожидание (§9.4).
- **D-W4-38** — `|W4| ≥ 0.382·|W3|`: волна 4 откатывает **не меньше 38.2%**
  волны 3 (константа `MIN_WAVE4_RETRACE_W3`). Откат мельче — импульсное
  продолжение тренда, а не сжатие клина: в настоящей диагонали волна 4
  возвращает заметную часть волны 3. Гейт — `w4RetraceTooShallow`,
  проверяется сразу после D-W4-78.
- **D-TIME-24** — `bars(W4) < bars(W2)`: волна 4 **короче волны 2 по
  длительности** (числу баров между концами волн). Если последний откат
  клина тянется дольше волны 2 — это дрейф/затяжной распил, а не сжатие
  перед развязкой: у зрелого клина финальный откат быстрее и компактнее.
  Гейт — `w4TimeNotContracting`, проверяется сразу после D-W4-38.
  Отключается флагом `RequireWave4Shorter` (по умолчанию включён) — осторожно:
  без него проходят многодневные «клины-монстры» (§9.12).
- **D-TIME-12** *(опционально, флаг `RequireWave2Shorter`, по умолчанию
  выключен)* — `bars(W2) < bars(W1)`: волна 2 **короче волны 1 по числу баров**.
  Продолжение цепочки «время сжимается вместе с ценой» (ср. D-TIME-24) на пару
  волн 1–2: у зрелого клина каждая следующая волна компактнее предыдущей не только
  по цене, но и по длительности. В определение диагонали правило не входит —
  контракция времени между импульсной волной 1 и коррекционной волной 2 не
  обязательна, поэтому это настройка качества, а не жёсткий фильтр. Гейт —
  `w2TimeNotContracting`, проверяется сразу после D-W2-RET.
- **D-OVERLAP** — `s·(V(4) − V(1)) < 0`: конец волны 4 **в ценовой зоне
  волны 1** (перекрытие; hard — решение O-2). Без перекрытия это импульс,
  а не диагональ.
- **D-W4-24** — `s·(V(1) − V(4)) ≥ 0.236·|W2|`: если принять за **0** конец волны 1
  (начало волны 2), а за **1** — конец волны 2, то волна 4 должна дойти
  **хотя бы до уровня 0.236** этого диапазона (параметр
  `MinWave4Wave2Level`, дефолт `MIN_WAVE4_W2_LEVEL` = 0.236; `0` — остаётся один
  D-OVERLAP). Это усиление D-OVERLAP: тот требует лишь уровня > 0,
  то есть формального захода в зону волны 1 на любую величину. Гейт —
  `w4ShallowIntoW2`, проверяется сразу после D-OVERLAP.
- **D-W4-2** — `s·(V(4) − V(2)) > 0`: волна 4 не пробивает конец волны 2
  (следствие сжатия, но проверяем явно — экстремумы могут разъехаться).
- **D-W5-BREAK** — волна 5 пробивает конец волны 3: `s·(H5 − V(3)) > 0`,
  где `H5` — бегущий экстремум волны 5 по сырым барам. Усечения исключены;
  это же — **триггер сигнала** (§6).
- **D-W5-CAP** — `|W5| < |W3|`: бегущая длина волны 5 меньше длины волны 3.
  Достижение 100% = инвалидация модели = уровень стопа (§6).
- **D-W5-78** *(опционально, флаг)* — на момент сигнала `|W5| ≥ 0.786·|W3|`:
  волна 5 уже «взрослая», вход ближе к теоретическому потолку → лучше R:R
  фактического разворота (§6.1).
- **D-CONTAIN** — внутри каждой из волн 1–4 цена не выходит за её концы
  (interior-бары, инвариант I2, как `IsWaveContained` в треугольниках).
- **D-TIME** — sanity длительностей **однохарактерных** волн: W3 против W1 и
  W4 против W2, `max(a/b, b/a) ≤ MaxWaveDurationRatio` (ловит волны, «сшитые»
  через разрывы, как в треугольниках). Порог — **параметр**, по умолчанию
  **8.0**: в диагонали коррекции и импульсы регулярно расходятся по времени
  в разы, прежние 4.0 резали валидные клинья (§9.8).
- **D-CONVERGE** *(параметр `MinConvergence`, по умолчанию **0**)* — образующие
  1-3 и 2-4 сходятся не слабее заданного (§4.2).
- **D-INSIDE** *(опционально, флаг `RequireInsideWedge`, по умолчанию
  **включено**)* — бары волн 2–4 не выходят за образующие надолго:
  площадь выброса ≤ `MaxSpillAreaRatio` от площади клина (§4.3).
- **D-INIT** *(опционально, флаг `RequireInitialDiagonal`)* — движение
  `V(0) → W5` (**вся** диагональ, включая волну 5 на баре сигнала) проходит тест
  `IsInitialMovement`: отматывая бары назад от точки 0, бар, зашедший дальше
  экстремума волны 5, встречается **раньше**, чем бар, вернувший цену за `V(0)`.
  То есть диагональ целиком укладывается внутрь предыдущего контр-движения, а не
  просто стартует «со свежего разворота». Это **контекстный**, а не
  типовой фильтр (§5.2): он не разделяет начальные и конечные диагонали.
  По умолчанию **выключено**; статистика — §9.4. Проверяется не на скелете, а
  **на сигнале** (§6): экстремум волны 5 существует только там. Это единственное
  правило, которое отматывает бары назад.

Soft-фильтры — **выключены на первом этапе** (решение O-7):

- фибо-соотношение `W3/W1` ∈ 0.5–0.786 (`CONTRACTING_DIAGONAL_3_TO_1`,
  EW_RULES.md §4.5). Сначала собираем статистику без него (§9.1) и только потом
  решаем, включать ли. В коде оставляем точку расширения (константы
  `W3_TO_W1_MIN/MAX` + флаг), чтобы включение было однострочным.

### 4.2. Сходимость образующих (`MinConvergence`, D-CONVERGE)

**Сжатие амплитуд не гарантирует сходимость трендлиний** — изначально
в этом документе было написано обратное, и это было ошибкой. Наклон
образующей зависит не только от цен, но и от **времени**.

В v-пространстве (`s = isUp ? 1 : −1`) легко показать, что

$$\text{наклон}_{1\text{-}3}=\frac{|W3|-|W2|}{t_3-t_1},\qquad
  \text{наклон}_{2\text{-}4}=\frac{|W3|-|W4|}{t_4-t_2}$$

(от `V(1)` до `V(3)` цена проходит вниз `|W2|` и вверх `|W3|`; от `V(2)` до
`V(4)` — вверх `|W3|` и вниз `|W4|`). Оба числителя положительны в силу
уже проверенных D-W3-PEN и D-CONTRACT-4. Клин **сходится** тогда и только
тогда, когда верхняя образующая растёт медленнее нижней:

$$\frac{|W3|-|W2|}{t_3-t_1}\;<\;\frac{|W3|-|W4|}{t_4-t_2}$$

Отсюда видно, что расхождение — это **не** просто «волна 4 больше/дольше
волны 2»:

- по **амплитуде** условие `|W4| < |W2|` уже обязательно (D-CONTRACT-4), то есть
  ваш вариант в этой части уже выполнен — и тем не менее клин может
  расходиться;
- по **времени** длительности входят не попарно (W4 против W2), а
  суммарно по пролётам образующих: `t3 − t1` (волны 2+3) против
  `t4 − t2` (волны 3+4). Длинная по времени волна 4 действительно толкает
  клин к расхождению (растёт `t4 − t2` → нижняя образующая положе), но
  это один из четырёх факторов, а не критерий.

Поэтому сходимость измеряется **числом**, а не проверяется флагом. Мера —
во сколько раз клин сузился от начала до конца скелета:

$$\boxed{\;c=\frac{w(t_1)}{w(t_4)}-1\;\ge\;\texttt{MinConvergence}\;}$$

где `w(t)` — расстояние между образующими на баре `t` (обе продолжены до
концов скелета; `w > 0` на всём `[t1, t4]`, см. ниже). Шкала:

| `c` | смысл |
|---:|---|
| `−1` | предел расхождения (`w(t4) → ∞`); мера всегда строго больше `−1` |
| `−0.5` | клин раскрылся вдвое |
| `0` | образующие **параллельны** |
| `+1` | клин **вдвое** уже в точке 4, чем в точке 1 |
| `+5` | вшестеро уже |

Свойства: безразмерно (отношение двух ценовых расстояний), не зависит от
масштаба символа, таймфрейма и длины клина. `MinConvergence = −1` полностью
отключает фильтр.

Отдельно проверять, что вершина клина правее точки 4, не нужно: при
`наклон_{1-3} > 0` верхняя образующая в точке `t4` всегда выше `V(3) > V(4)`,
то есть линии ещё не пересеклись, и обе ширины положительны.

> **По умолчанию `MinConvergence = 0`** — отбрасываются только реально
> расходящиеся клинья. Это стоит ~3% входов и улучшает матожидание во всех
> без исключения режимах. Ужесточение работает как регулятор качества:
> `+0.5` и `+1.0` заметно поднимают матожидание ценой выборки (§9.6).

### 4.3. Бары внутри клина (`RequireInsideWedge`, D-INSIDE)

Наблюдение: если бары подволн **надолго** вылезают за образующие, сетап
отрабатывает хуже — вероятно, это вообще не диагональ, а неудачно нарезанный
тренд или расширяющаяся структура. При этом одиночные прострелы хвостами —
норма: пивоты касаются линий, а хвосты вокруг них шумят.

Значит, нужен критерий, который **различает единичный выброс и долгий выход**,
то есть измеряет **площадь**, а не факт нарушения. Метрика — «доля площади
клина, проведённая снаружи»:

Для каждого бара `i ∈ [t1, t4]` (в v-пространстве, где линия 1-3 — всегда
потолок, а 2-4 — пол; линии продолжаются за свои опорные точки):

$$o_i=\max\bigl(0,\;\max_i-U_i\bigr)+\max\bigl(0,\;L_i-\min_i\bigr),\qquad
  w_i=U_i-L_i>0$$

$$\boxed{\;A=\frac{\sum_i o_i}{\sum_i w_i}\;\le\;\texttt{MaxSpillAreaRatio}\;}$$

где `U_i`/`L_i` — значения образующих на баре `i`, а `max_i`/`min_i` — high/low
этого бара. Свойства, ради которых выбрана именно эта форма:

- **безразмерность** — и числитель, и знаменатель в цене, поэтому порог один
  и тот же для EURUSD и XAUUSD, для m15 и h1, для крупных и мелких клиньев;
- **нормировка на длину** — оба слагаемых суммируются по одним и тем же барам,
  так что длинный клин не штрафуется за то, что он длинный;
- **ровно то разделение, которое нужно**: одна свеча, вылезшая на пол-ширины
  канала, на пролёте в 60 баров даёт `A ≈ 0.5/60 ≈ 0.008`; двадцать баров,
  сидящих на 30% снаружи, дают `A ≈ 0.10` — на порядок больше. Порог режет
  второе, не трогая первое;
- **учитывается и потолок, и пол** одной формулой, знак берётся из v-пространства.

Отдельный лимит «на одну свечу» не вводим: он бы вернул именно ту жёсткость,
от которой мы уходим. Гейт — `spillsOutOfWedge`, проверяется после дешёвой
геометрии (единственный проход по всем барам скелета).

`MaxSpillAreaRatio = 0.005` по умолчанию — откалибровано на архиве (§9.7).
Порог именно такой малый потому, что нормальная диагональ держится внутри
клина почти идеально, и «выброс» в 0.5% площади — это уже заметный горб.

### 4.4. Изолированные принты (исключение «плохих» баров из анализа)

Наблюдение (§9.13): в данные попадает одиночный «принт» — свеча, цена которой
далека от диапазона соседних баров и к которой рынок больше никогда не
вернулся (типичная причина — ошибочное исполнение / одиночная сделка в тонком
стакане). Зигзаг уверенно подхватывает такой бар как пивот, и модель
строится на цене, которую рынок фактически не торговал: в кейсе §9.13 конец
волны 4 встал на принт, и клин оказался фикцией. Зигзаг при этом трогать
нельзя — он детерминирован и должен таким остаться; фильтруется **потребление**
его пивотов и сырых баров.

**Определение.** Сегмент из `1..MaxSpikeBars` последовательных баров —
*изолированный принт*, если одновременно выполнены три условия:

1. **Двусторонний гэп (ядро критерия).** Ценовой диапазон сегмента не
   пересекается с диапазонами ни предыдущего, ни следующего бара: для принта
   вниз `maxHigh(seg) < low(prev)` **и** `maxHigh(seg) < low(next)` (вверх —
   зеркально). Это масштабно-инвариантная подпись «цены, которую не
   торговали»: резкое, но реальное движение оставляет пересечение диапазонов
   хотя бы с одной из сторон.
2. **Значимость смещения.** Глубина отрыва `D` — расстояние от экстремума
   сегмента до ближайшей проторгованной границы соседних баров — не меньше
   `MinDisplacementAtr × локальная волатильность` (медиана диапазонов
   последних 50 баров перед сегментом). Дефолт **4.0**: кейс §9.13 даёт
   ≈ 14 медианных диапазонов, запас многократный; порог защищает от тиковых
   непересечений на мёртвом рынке.
3. **Быстрый возврат (подтверждение).** В течение `RetraceWindowBars` баров
   после сегмента цена возвращает не меньше доли `MinRetraceShare` смещения
   `D` (дефолты — 12 баров и 0.8). Отличает плохой принт от настоящего флеш-
   крэша с продолжением, где цена остаётся жить в новой зоне.

Сегмент подтверждается с **задержкой** `RetraceWindowBars` баров — он
становится «исключённым» только когда окно возврата закрылось. Это приемлемо:
конец волны 4 попадает в модель лишь после того, как волна 5 уже развивается,
и задержка много короче жизни модели. Смежные принт-бары склеиваются в один
сегмент (перебор начинается с самых длинных сегментов).

**Что критерий сознательно НЕ ловит:**

- сессионные и выходные гэпы — гэп односторонний (диапазон бара перед
  разрывом пересекается с сегментом), условие 1 не срабатывает; кросс-скейл
  механика выходных (§7.2) продолжает работать;
- новостные свечи — диапазоны соседей пересекаются и/или нет быстрого возврата;
- обычные хвостовые пики — их экстремумы проторгованы внутри диапазонов соседей.

Серая зона — настоящий флеш-крэш с полным V-образным возвратом. Разрешается в
пользу «модель на сомнительном принте не строим»: пропустить один сетап
дешевле, чем торговать стоп, отталкивающийся от фиктивного экстремума.

**Интеграция в финдер.** Зигзаг не модифицируется; фильтрация — на точках
потребления:

- пивоты, чей бар попал в подтверждённый сегмент, выбрасываются из хвоста
  (`CachedTailPivots`) — скелет на принте просто не собирается;
- уже зарегистрированные кандидаты, чей скелет стоит на принте, снимаются с
  сопровождения в момент подтверждения сегмента (счётчик воронки
  `excludedPrintSkeleton`) — регистрация может опередить подтверждение:
  пивот возникает по самому факту возврата цены, а сегменту нужно полное окно;
- волна 5 ведётся по сырым барам, пропуская исключённые (`AdvanceWave5`):
  принт не двигает `W5Extreme` и не инвалидирует кандидата пробоем `V(4)`;
- D-CONTAIN и D-INSIDE пропускают исключённые бары, чтобы принт не ломал
  контенмент и не раздувал площадь выброса.

Компонент — [`IsolatedPrintFilter`](TradeKit.Core/Indicators/IsolatedPrintFilter.cs),
бар-уровневый, каузальный, переиспользуем для треугольников/импульсов/гармоников.
Дефолты: `MaxSpikeBars = 3`, `MinDisplacementAtr = 4.0`,
`RetraceWindowBars = 12`, `MinRetraceShare = 0.8`. В конструктор финдера
фильтр передаётся опциональным параметром (`null` — экземпляр по умолчанию,
то есть фильтр **включён**; `MaxSpikeBars ≤ 0` — выключен).

---

## 5. Откуда считать волну 1 (проблема точки 0)

### 5.1. Зачем вообще нужна точка 0

Триггер сетапа — события волн 4–5, а точка 0 нужна **только для того, чтобы
измерить волну 1** и тем самым проверить **сомасштабность волн 1 и 3**
(решение O-6). От `V(0)` зависят ровно два правила:

- **D-W2** — волна 2 не уходит за начало волны 1;
- **D-CONTRACT-3** — `|W3| < |W1|` (и, в перспективе, soft-фибо `W3/W1`, §4).

Всё остальное (перекрытие W4↔W1, сжатие `|W4|<|W2|`, пробой и потолок волны 5,
стоп и тейк) считается **без точки 0**. Поэтому ошибка в точке 0 не разрушает
сетап целиком — она лишь ослабляет проверку сомасштабности.

### 5.2. Кандидаты в точку 0 — пивоты своего рунга, без «заходности»

1. **Кандидат — каждый подтверждённый пивот** нужного знака (v-low для `isUp`)
   на каждом рунге лестницы периодов (§8). Крупные диагонали резолвятся на
   крупном рунге, мелкие — на тонком; дубли схлопываются дедупликацией по
   точке 0.
2. Волна 1 собирается тем же жадным слиянием подволн, что и волны 2–4
   (§7.2), поэтому точка 0 — это просто пивот, с которого волна 1 стартует.
3. **Тест `IsInitialMovement` по умолчанию выключен** (решение O-6). Он требует,
   чтобы перед точкой 0 было контр-движение, покрывающее **всю** диагональ, —
   высотой ≥ |W5-экстремум − V(0)| на момент сигнала, — то есть чтобы диагональ
   целиком шла назад внутрь предыдущего движения. Для **начальной** диагонали
   (волна 1/A
   нового движения, стартует прямо из конца предыдущего тренда) это далеко
   не всегда верно. Мы ищем оба типа (O-5), поэтому гейт не может быть
   обязательным — тот же урок, что и с волной A треугольников
   (EW_R_TRIANGLE.md §2: этот гейт убивал ~80% кандидатов).
4. **Чего гейт НЕ делает** — он не разделяет начальные и конечные диагонали.
   «Свежий разворот» перед точкой 0 бывает у конечной диагонали в волне 5
   импульса, но точно так же — у конечной диагонали в **волне C плоскости**
   (волна B — готовое контр-движение перед точкой 0), в волне C зигзага и
   у части начальных диагоналей после глубокой коррекции. Гейт отбирает
   только контекст старта (чистый разворот против «продолжения с хода»),
   а последующая цель у этих случаев разная (после волны C плоскости идёт
   продолжение старшего тренда, а не обычная коррекция), поэтому выборка
   под флагом остаётся разнородной — что и видно по статистике §9.4.

> Отбор «мусорных» точек 0 обеспечивают сами структурные правила §4:
> случайно выбранная точка 0 почти всегда ломает либо D-W2, либо
> D-CONTRACT-3.

### 5.3. Cross-scale сборка

Как и у running-треугольника (EW_R_TRIANGLE.md §7.6), скелет и триггер живут
на разных масштабах:

- **точка 0 и волны 1–4** — пивоты рунга, на котором они чисто резолвятся
  (перебор по лестнице, коарсест-фёрст); точка 0 нужна только для измерения
  волны 1 (§5.1);
- **волна 4 может быть «плавающей»** — её конец двигается на новый экстремум,
  пока не начался пробой (аналог floating C у треугольника);
- **волна 5 целиком по сырым барам**: от бара конца W4 трекаем бегущий экстремум
  `H5`; события «пробит V(3)», «достигнуто 0.786·|W3|», «достигнуто |W3|»
  проверяются на каждой закрытой свече, без ожидания пивота зигзага.

---

## 6. Сигнал (entry / TP / SL)

Все решения по механике подтверждены (§11). Для `isUp` (бычья диагональ → SELL):

- **Триггер**: закрылась свеча, чей high пробил `V(3)` (пробой хвостом
  достаточен, закрытие за уровнем не требуется — решение O-3), при этом волны
  1–4 собраны и ни одно правило §4 не нарушено.
- **Вход**: market по цене закрытия триггерной свечи, **против** диагонали.
- **Stop-Loss**: `SL = V(4) + |W3|` (для `isUp`; зеркально `V(4) − |W3|`) —
  теоретический потолок волны 5 в сжимающейся диагонали. Плюс
  `PERCENT_ALLOWANCE_SL`, округление до `Symbol.Digits`. Пробой SL =
  одновременно инвалидация модели (D-W5-CAP), лишних «сирот»-стопов нет.
- **Take-Profit**: зависит от режима `TakeProfitMode` (§6.3). По умолчанию
  (`RISK_RATIO`) — `TP = entry − R·(SL − entry)`, где `R = TakeProfitRatio`
  (параметр, по умолчанию **1.0** — решение O-4; `R = 2` даст цель вдвое
  дальше стопа). TP считается от **уже округлённого** SL, поэтому
  фактический R:R точно равен заданному (отдельного `PERCENT_ALLOWANCE_TP`
  нет — он ломал бы ровное соотношение).
- **Отбраковка**: сигнал не выдаётся, если на триггерной свече TP или SL уже
  задет, либо риск вырожденно мал:
  `|SL − entry| < MIN_RISK_TO_W3_RATIO·|W3|`. Последнее важно: чем ближе волна 5
  к потолку `|W3|`, тем тоньше стоп — вырожденный стоп даёт фантастический
  R:R на бумаге и мгновенный вынос на практике.
- **Дедупликация — по точке 0** + TP/SL-подпись (`SignalKey`): одна диагональ =
  один сигнал, дубли между рунгами лестницы схлопываются.

### 6.1. Опция «зрелая волна 5» (`RequireWave5Ratio`)

Флаг (по умолчанию **выключен**). Если включён, сигнал выдаётся только когда
**оба** условия выполнены на закрытой свече:

1. пробой `V(3)` (обязателен всегда);
2. `|W5| ≥ WAVE5_MIN_RATIO·|W3|` (константа 0.786).

Если пробой V(3) случился раньше отметки 78.6% (глубокая волна 4), кандидат
ждёт в фазе `ARMED_WAIT` — сигнал сработает на той свече, где длина волны 5
дотянется до 78.6%. Если раньше этого волна 5 достигнет 100% |W3| — кандидат
инвалидируется (D-W5-CAP), сигнала не будет.

> Смысл опции: вход ближе к теоретическому потолку волны 5 → короче путь до
> стопа при том же стопе → фактический R:R разворота лучше. Плата — часть
> диагоналей развернётся, не дотянув до 78.6%, и сигнал будет пропущен.
> Сравнение обоих режимов — задача самотеста (§9).

### 6.2. Пост-сигнальное сопровождение

После входа сопровождение стандартное (TP/SL-хиты по барам, как во всех
финдерах). Отдельной пост-сигнальной инвалидации, как у треугольника
(`invalidatedE`), не требуется: единственный сценарий «модель умерла» —
волна 5 длиннее волны 3 — совпадает с уровнем SL по построению.

### 6.3. Режимы тейк-профита (`TakeProfitMode`)

Два варианта цели (третий — §6.6); выбор — параметр финдера.

- **`RISK_RATIO`** *(по умолчанию)* — фиксированный R:R от стопа (§6).
  Цель не привязана к геометрии движения, зато статистика читается напрямую.
- **`DIAGONAL_RETRACE`** — **D-TP-236**: цель на уровне коррекции **23.6% всей
  диагонали**: `TP = W5 ∓ 0.236·|W5 − V(0)|` (знак — против диагонали), где
  `W5` — бегущий экстремум волны 5 на триггерной свече. `TakeProfitRatio`
  игнорируется, **R:R становится переменным** и определяется геометрией
  клина: чем выше диагональ относительно остатка волны 5 до потолка `|W3|`,
  тем больше R. Логика: минимальная коррекция после диагонали — это
  процент от самого движения, а не от того, где случайно встал стоп.
  Дополнительный гейт `tpBehindEntry`: если триггерная свеча закрылась уже
  за уровнем 23.6% (длинный откат внутри свечи), сигнал не выдаётся.
  Плавающий R:R можно ограничить снизу параметром `MinRiskRewardRatio` —
  тогда невыгодный сетап не отбрасывается, а ждёт улучшения (§6.5).

### 6.4. Переоценка уровня 23.6% после входа (`RetraceAction`)

**Проблема.** В момент сигнала волна 5 ещё не закончилась: мы входим на пробое
`V(3)`, а экстремум `H5` продолжает расти. Уровень 23.6% считается **от него**,
поэтому зафиксированный на триггерной свече TP — это оценка по неполным данным.
Если после входа цена ушла в сторону стопа, но его не достигла, «настоящий»
уровень 23.6% сдвигается **ближе ко входу**:

$$L(H_5)=H_5-s\cdot0.236\cdot|H_5-V(0)|,\qquad
  \frac{\partial L}{\partial H_5}=s\cdot0.764>0$$

то есть для бычьей диагонали (сделка SELL) новый уровень **выше** исходного TP.
Минимальная коррекция, ради которой мы вошли, фактически уже отработана — но по
старому TP это не видно.

**Решение.** Пока сетап открыт, `H5` продолжает обновляться (по закрытым барам —
в индикаторе и бэктесте, по тикам — в роботе), уровень `L(H5)` пересчитывается,
и когда он **достигнут** и при этом лежит **на прибыльной стороне от входа**,
один раз срабатывает действие `RetraceAction`:

| значение | что делает |
|---|---|
| `NONE` *(по умолчанию)* | ничего, TP не корректируется — прежнее поведение |
| `BREAKEVEN` | сигнал на безубыток: SL переносится на цену входа |
| `BREAKEVEN_AND_HALF` | безубыток + закрытие половины позиции по рынку |
| `HALF` | только закрытие половины позиции, стоп не трогаем |
| `CLOSE` | полное закрытие позиции по текущей цене |

Существенные детали:

- **TP не двигается ни в одном из режимов.** Опция управляет позицией, а не
  разметкой: пересчитанный уровень — это триггер, а не новая цель. Иначе TP
  «уползал» бы вслед за каждым новым экстремумом волны 5, а сетап ничем бы не
  отличался от трейлинга.
- **Срабатывает один раз за сетап** (флаг `m_RetraceActionFired`). Волна 5 может
  сделать ещё несколько экстремумов, но повторно фиксировать нечего: половина уже
  закрыта, стоп уже в безубытке.
- **Условие прибыли обязательно**: если `L(H5)` оказался за входом (волна 5 ушла
  так далеко, что 23.6% от неё не доходит до цены входа), действие **не**
  выполняется — иначе «безубыток» и «фиксация половины» происходили бы в минусе.
- **Не зависит от `TakeProfitMode`.** В режиме `DIAGONAL_RETRACE` это прямая
  переоценка собственной цели; в режиме `RISK_RATIO` — независимый триггер
  «минимальная коррекция после диагонали достигнута». Код один и тот же.
- **После `BREAKEVEN*` финдер сам ведёт стоп в безубытке**: возврат цены ко входу
  закрывает сетап событием `OnManualClose` по цене входа (результат ≈ 0R), а не
  исходным `OnStopLoss` (−1R). Без этого бэктест и статистика показывали бы
  убыток там, где робот вышел в ноль, и сравнить режимы было бы нельзя.

**Реакция по тикам vs по барам.** Триггер проверяется в двух местах одним и тем
же методом `ManageOpenSetup`:

| контур | где | цена |
|---|---|---|
| закрытая свеча | `HandleOpenSetup` (из `CheckSetup`) | high/low бара |
| тик | `CheckTick` (роутинг уже есть: `BaseAlgoRobot.TradeManagerOnTick` → `sf.CheckTick`) | bid для лонга, ask для шорта |

Индикатор тики не получает (`BaseIndicator.Calculate` вызывает только
`CheckBar`), поэтому там всё сводится к сообщению в лог на закрытии свечи,
пробившей новый уровень — как и требовалось. Робот реагирует на первом же тике.

**Что происходит на стороне робота и отчётов.** Отдельного события не вводится,
используется существующая связка:

- `HALF` / `BREAKEVEN*` → `OnBreakeven` с флагами `CloseHalf` и **новым**
  `MoveStopToEntry` в `LevelEventArgs`. `BaseAlgoRobot.ModifySymbolPositions`
  вызывает `TradeManager.ClosePartial(position, 0.5)` и/или
  `TradeManager.SetBreakeven(position)`;
- `CLOSE` → `OnManualClose` по достигнутому уровню; штатный
  `TelegramReporter.ReportManualClose(posId, price)` считает статистику **по
  фактической цене выхода**, поэтому переоценка положения фибо-уровня
  автоматически отражается как результат меньше запланированного.

**Статистика при частичном закрытии.** `TelegramReporter.SignalArgs` получает два
поля — `ClosedPart` (уже закрытая доля позиции) и `ClosedResult` (реализованный
на ней R). `ReportPartialClose` дописывает их в момент фиксации половины, а
итоговый `GetStatistic` считает взвешенный результат

$$R=\underbrace{R_\text{закрыт}}_{\texttt{ClosedResult}}+
  \bigl(1-\texttt{ClosedPart}\bigr)\cdot\frac{|\,\text{entry}-\text{exit}\,|}{|\,\text{entry}-SL\,|}$$

и по-прежнему делает **один** вызов `AddSetupResult` на сетап: одна модель —
одна запись в статистике, но с честным R.

Тренажёр (§9.2) опцию не использует: она управляет уже открытой позицией и на
вопрос «TP или SL» не влияет.

### 6.5. Ожидание нужного R:R (`MinRiskRewardRatio`)

**Проблема.** В режиме `DIAGONAL_RETRACE` цель — геометрический уровень, а не
множитель риска, поэтому R:R сетапа **плавает** (§6.3). Если пробой `V(3)`
случился рано (волна 5 едва отклонилась от `V(4)`), стоп `V(4) ± |W3|` ещё
далеко, а уровень 23.6% — уже близко: сделка с R:R ≈ 0.3 математически хуже,
чем отсутствие сделки.

**Решение.** Параметр `MinRiskRewardRatio` (доля, по умолчанию **0** —
логика выключена, поведение прежнее). Если на триггерной свече

$$\frac{|TP-\text{entry}|}{|\text{entry}-SL|}<\texttt{MinRiskRewardRatio},$$

сигнал **не выдаётся, но кандидат не умирает**: он паркуется в состоянии
ожидания и проверяется заново **на каждой закрытой свече**. Оба конца
неравенства при этом двигаются:

- `entry` — цена закрытия текущей свечи, она уходит вглубь волны 5, то есть
  **ближе к стопу** → знаменатель уменьшается;
- `TP` — пересчитанный уровень `W5 ∓ 0.236·|W5 − V(0)|` по **свежему**
  экстремуму волны 5 (та же переоценка, что в §6.4, только до входа, а не
  после) → числитель растёт вместе с диагональю.

Как только неравенство перестаёт выполняться, сигнал выдаётся обычным
порядком — с ценой входа и целью **этой** свечи, а не триггерной.

Существенные детали:

- **Работает только в режиме `DIAGONAL_RETRACE`.** В `RISK_RATIO` R:R задан
  параметром `TakeProfitRatio` и ждать нечего.
- **Ожидание ограничено самой моделью**: кандидат в ожидании продолжает
  проходить §7.3, поэтому умирает по D-W5-CAP (`|W5| ≥ |W3|`), по D-TIME или
  при уходе цены за `V(4)`. Отдельного таймаута не нужно.
- **Реакция по закрытию свечи** (в отличие от §6.4, где робот реагирует ещё и
  по тикам): позиции нет, спешить некуда, а вход по закрытию свечи — базовая
  механика финдера (§6). Перевод на тики — возможное будущее уточнение
  (по аналогии с `ManageOpenSetup`).
- **Флаг `WasTriggerable` перестаёт быть «однократным» только для ждущих
  кандидатов**: обычный кандидат по-прежнему стреляет ровно на первой свече,
  где условия §7.3 выполнились, — иначе один и тот же клин порождал бы сигнал
  на каждой свече.
- Отбраковка попадает в диагностику как гейт `ratioTooLow` (§9.4).

### 6.6. Цель от волны 3 (`Wave3RetraceRatio`)

**D-TP-W3.** Тот же геометрический подход, что и §6.3, но откат меряется **от
волны 3**, а не от всей диагонали:

$$TP = W_5 \mp r\cdot|W_3|,\qquad r = \texttt{Wave3RetraceRatio}$$

где `W5` — бегущий экстремум волны 5 на триггерной свече, знак — против
диагонали. По умолчанию **0** — режим выключен, цель ставится по
`TakeProfitMode` (прежнее поведение); например `0.382` — TP на уровне отката
38.2% волны 3.

Существенные детали:

- **Перекрывает `TakeProfitMode`.** При `r > 0` цель считается только по этой
  формуле — и `TakeProfitRatio`, и режим `DIAGONAL_RETRACE` игнорируются.
- **Зачем отдельный базис.** Уровень 23.6% всей диагонали зависит от точки 0,
  а она — самый шаткий элемент разметки (§5, §9.11). `|W3|` измеряется внутри
  клина и к выбору точки 0 не чувствителен; кроме того, `|W3|` — тот же
  масштаб, что и у стопа (`SL = V(4) ± |W3|`), поэтому R:R плавает заметно
  меньше.
- **Гейты те же, что у §6.3**: `tpBehindEntry` (триггерная свеча закрылась уже за
  уровнем) и ожидание нужного R:R по `MinRiskRewardRatio` (§6.5) — цель
  геометрическая, значит R:R плавающий.
- **`RetraceAction` (§6.4) не меняется**: там по-прежнему пересчитывается уровень
  23.6% всей диагонали — это независимый триггер управления позицией, а не
  цель.

---

## 7. Инкрементальный алгоритм (жизненный цикл кандидата)

Архитектура повторяет `RunningTriangleSetupFinder`: **stateful**, кандидаты
живут между барами. Отличие одно, но принципиальное: триггер — событие
**бара**, а не пивота, поэтому работа разделена на два контура с разной
частотой:

| Контур | Когда | Стоимость |
|---|---|---|
| Сборка скелета 0-1-2-3-4 по пивотам | только когда список пивотов рунга **изменился** (вырос `Count` либо сдвинулся последний экстремум) | дорого, но редко |
| Ведение волны 5 по сырым барам | каждый бар, для всех живых кандидатов | O(1) на кандидата |

### 7.1. Состояние кандидата

```
DiagonalCandidate {
    BarPoint Point0, P1, P2, P3, P4;  // скелет, уже прошедший §4 (правила без W5)
    bool     IsUp;                    // направление ДИАГОНАЛИ (торгуем против)
    double   W5Extreme;               // бегущий экстремум волны 5 (сырые бары)
    int      W5ExtremeBar;            // бар этого экстремума
    bool     WasTriggerable;          // выполнялись ли условия сигнала ДО текущего бара
    bool     IsWaitingForRatio;       // сетап годен, но R:R ниже MinRiskRewardRatio (§6.5)
}
```

Ключ кандидата — `(Point0.OpenTime, P4.OpenTime, IsUp)`: повторная сборка того же
скелета (в т.ч. на соседнем рунге лестницы) не создаёт дубликата и не
сбрасывает уже набранную волну 5. Флаг `WasTriggerable` гарантирует сигнал
ровно на **первой** свече, где условия выполнились.

### 7.2. Порождение (по изменению пивотов рунга)

Конец волны 4 — это **последний** пивот рунга (пока волна 5 ещё не
зарегистрирована как экстремум) либо предпоследний (когда волна 5 уже
отклонилась на `deviation%` и породила свой плавающий экстремум). Оба
варианта проверяются. Для каждого из них:

1. направление `isUp` выводится из знака пивота (конец W4 бычьей
   диагонали — локальный минимум);
2. перебор кандидатов в точку 0 вглубь на `MAX_ASSEMBLY_DEPTH` пивотов;
3. волны 1–4 собираются жадным слиянием подволн (`ExtendWave`, тот же
   алгоритм, что у треугольников); конец волны 4 обязан совпасть со выбранным
   пивотом. Если слияние **обрывает** волну (обычно глубокую волну 2, см. §9.9),
   срабатывает фолбэк `TryCrossScaleWave2`: волны 0-1 остаются на рунге-событии,
   конец волны 2 ищется на каждой более грубой ступени лестницы, волны 3-4
   вырезаются заново на любой ступени, резолвящей волну 4 как свежий пивот.
   Исключение — конец волны 1, который грубая ступень **вообще не видит**
   (двойная вершина: жадное слияние со строгим `>` оставляет экстремумом
   первое касание, а грубый масштаб резолвит только второе, см. §9.10): в этом
   случае волна 1 вырезается заново от точки 0 на грубой ступени. Если и
   кросс-масштабный путь ничего не дал, срабатывает `TryShorterWave1`: конец
   волны 1 перебирается по ближайшим **более ранним** пивотам того же рунга —
   лечит «перелитую» волну 1 при мелкой волне 2 (§9.11);
4. прогоняются все правила §4, кроме W5-шных → кандидат в пул.

### 7.3. Ведение волны 5 (каждый бар, сырые бары)

Для каждого кандидата пула на закрытой свече:

1. если цена ушла за `V(4)` против направления диагонали → `DEAD` (конец W4
   ещё не сформирован; новый кандидат с более глубоким P4 родится сам);
2. обновить `W5Extreme` по high/low текущего бара;
3. `|W5| ≥ |W3|` → `DEAD` (D-W5-CAP);
4. длительность волны 5 нарушила D-TIME → `DEAD`;
5. условие сигнала = пробой `V(3)` **и** (если `RequireWave5Ratio`)
   `|W5| ≥ 0.786·|W3|`. Сигнал выдаётся, когда условие выполнилось сейчас и
   не выполнялось до этой свечи (`WasTriggerable == false`) — либо когда
   кандидат ждёт нужного R:R (`IsWaitingForRatio`, §6.5), и тогда попытка
   повторяется на каждой свече.

### 7.4. `CheckSetup(openDateTime)` — план

1. Отработать активный сетап (TP/SL-хиты).
2. Обновить лестницу зигзагов; на рунгах с изменившимся списком пивотов —
   прогнать §7.2.
3. Прогнать §7.3 по всему пулу, убрать `DEAD`.
4. Первый сработавший кандидат → `OnEnterInvoke` с `ElliottWaveSignalEventArgs`
   (WavePoints = [0,1,2,3,4,5]).

---

## 8. Константы и параметры

| Имя | Назначение | Стартовое значение |
|---|---|---|
| `Period` (EWParams) | Базовый период зигзага; 0 → авто. | 0 (авто) |
| `TakeProfitRatio` | Множитель TP от дистанции до стопа (R:R). | **1.0** |
| `TakeProfitMode` | `RISK_RATIO` или `DIAGONAL_RETRACE` (D-TP-236, §6.3). | `RISK_RATIO` |
| `DIAGONAL_RETRACE_RATIO` | Доля коррекции диагонали для TP. | 0.236 |
| `RetraceAction` | Что делать при достижении пересчитанного уровня 23.6% (§6.4). | `NONE` |
| `MinRiskRewardRatio` | Мин. R:R сетапа в режиме `DIAGONAL_RETRACE`; ниже — ждём (§6.5). 0 — выключено. | **0** |
| `Wave3RetraceRatio` | TP на откате доли \|W3\| от экстремума волны 5 (D-TP-W3, §6.6). 0 — выключено. | **0** |
| `RequireWave5Ratio` | Требовать `|W5| ≥ 0.786·|W3|` на сигнале (§6.1). | `false` |
| `WAVE5_MIN_RATIO` | Порог зрелости волны 5. | 0.786 |
| `RequireWave4Ratio` | Требовать `|W4| ≥ 0.786·|W2|` (D-W4-78, §4). | `false` |
| `WAVE4_MIN_RATIO` | Нижняя граница `|W4|/|W2|`. | 0.786 |
| `MIN_WAVE4_RETRACE_W3` | Мин. откат волны 4 от волны 3 (hard, D-W4-38, §4). | **0.382** |
| `MIN_WAVE4_W2_LEVEL` | Мин. уровень волны 4 в диапазоне волны 2 (D-W4-24, §4) — дефолт параметра `MinWave4Wave2Level`. | **0.236** |
| `RequireWave4Shorter` | Требовать `bars(W4) < bars(W2)` (D-TIME-24, §4). | **`true`** |
| `RequireWave2Shorter` | Требовать `bars(W2) < bars(W1)` (D-TIME-12, §4). | `false` |
| `MinWave2Retrace` | Мин. откат волны 2 от волны 1 (доля \|W1\|, D-W2-RET, §4). 0 — выключено. | **0** |
| `RequireInitialDiagonal` | Требовать «заходности» всей диагонали `V(0) → W5` (D-INIT, §5.2). | `false` |
| `MinConvergence` | Минимальная сходимость образующих (D-CONVERGE, §4.2). | **0** |
| `RequireInsideWedge` | Требовать, чтобы бары не выходили за образующие (D-INSIDE, §4.3). | **`true`** |
| `MaxSpillAreaRatio` | Порог площади выброса (доля площади клина). | **0.005** (§9.7) |
| `MinWave3Penetration` | Мин. пробой W3 за W1 (доля \|W1\|), D-W3-PEN. | **0.03** (§9.8) |
| `MaxWaveDurationRatio` | Sanity длительностей однохарактерных волн (D-TIME). | **8.0** (§9.8) |
| `IsolatedPrintFilter` | Детектор изолированных принтов (§4.4); `null` в конструкторе → экземпляр по умолчанию. | **включён** |
| `MaxSpikeBars` | Макс. длина сегмента принта, баров. | **3** |
| `MinDisplacementAtr` | Мин. глубина отрыва `D` в медианных диапазонах последних 50 баров. | **4.0** |
| `RetraceWindowBars` | Окно подтверждения быстрого возврата; оно же — задержка, баров. | **12** |
| `MinRetraceShare` | Доля `D`, которую цена должна вернуть в окне. | **0.8** |
| `MIN_RISK_TO_W3_RATIO` | Мин. риск (SL−entry) как доля \|W3\| — отсев вырожденных стопов. | 0.05 |
| `MAX_ASSEMBLY_DEPTH` | Глубина перебора кандидатов в точку 0 (пивоты). | 40 |
| `LADDER_RATIOS` | Лестница периодов. | как в `RunningTriangleSetupFinder` |
| `PERCENT_ALLOWANCE_SL` | Аллоуанс SL. | `Helper.*` (как сейчас) |

---

## 9. Самотест по архивным CSV

Два контура проверки — автоматический (NUnit) и визуальный (ReplayViewer),
по образцу треугольников.

### 9.1. NUnit-тесты (`TradeKit.Tests/DiagonalSetupFinderTests.cs`)

Данные — репозиторные [data/*.csv](data/) (поиск каталога через `FindDataDir()`:
подъём от `TestContext.CurrentContext.TestDirectory` до папки с `data/` +
`TradeKit.sln`). Финдер прогоняется по-барно (causal), как в
`RunningTriangleSetupFinderTests`.

**Инварианты каждого выданного сигнала** (assert в цикле по `OnEnter`):

- волны упорядочены по времени, направления чередуются;
- сжатие: `|W3| < |W1|`, `|W4| < |W2|`;
- откат волны 2: `|W2| ≥ MinWave2Retrace·|W1|` (D-W2-RET);
- коридор волны 4: `s·(V(1) − V(4)) ≥ 0.236·|W2|` (D-W4-24) и `|W4| ≥ 0.382·|W3|`
  (D-W4-38);
- сходимость образующих `w(t1)/w(t4) − 1 ≥ MinConvergence` (D-CONVERGE);
- площадь выброса за образующие ≤ `MaxSpillAreaRatio` (D-INSIDE) — тоже всегда;
- перекрытие: конец W4 в зоне W1 (D-OVERLAP);
- пробой: на триггерном баре экстремум за `V(3)` (D-W5-BREAK);
- `SL == V(4) ± |W3|` (с точностью до аллоуанса/округления);
- `TP` соответствует `TakeProfitRatio`, а в режиме `DIAGONAL_RETRACE` — уровню
  `W5 ∓ 0.236·|W5 − V(0)|`;
- сделка против диагонали (бычья диагональ → TP ниже входа);
- при `RequireWave5Ratio` — `|W5| ≥ 0.786·|W3|` на триггерном баре;
- при `RequireWave4Ratio` — `|W4| ≥ 0.786·|W2|`;
- при `RequireInitialDiagonal` — сигналы остаются валидными и их не больше,
  чем без флага (гейт только сужает выборку).

**Funnel-диагностика**: research-тест `[Explicit, Category("Research")]` (по
образцу `PeriodSweepTests.DiagnoseTriangleRejections`) гоняет финдер по
нескольким файлам (m15 + h1) и пишет `reports/diagonal_rejections.md` —
счётчики `Diag` по гейтам (`w2Breach`, `noContract3`, `noOverlap`,
`w5Cap`, `tooCloseToSl`, `entered`, …). Это главный инструмент поиска
«гейта-убийцы» до какой-либо ручной калибровки (урок треугольников: один
неуместный гейт убивал 80% кандидатов).

**Сравнение режимов**: тот же research-тест прогоняет все комбинации
`MinConvergence` (−1 / 0 / +0.5 / +1) × `RequireWave5Ratio` × `RequireWave4Ratio` ×
`RequireInitialDiagonal` (false/true) и цели (`TakeProfitRatio`
1.0 / 1.5 / 2.0 / 3.0 плюс отдельный прогон `DIAGONAL_RETRACE`) и пишет
сводку TP/SL-исходов — эмпирический выбор дефолтов. Матожидание
считается по **фактическому R каждой сделки** (в retrace-режиме он
плавающий), в отчёт выводится средний `avgR`.

**Референсный пример** — по образцу EW_R_TRIANGLE.md §12: после первых прогонов
выбираем на архивном CSV реальную диагональ, фиксируем якоря (точка 0, точка 5,
направление, триггерная свеча) и закрепляем тестом. Кандидатов ищем визуально
через страницу §9.2. *(Раздел заполняется после первой реализации.)*

### 9.2. Страница самопроверки в ReplayViewer (quiz) — реализовано

По образцу running-треугольника (`rtriangle.html`):

- **[DiagonalScanEngine.cs](TradeKit.ReplayViewer/Services/DiagonalScanEngine.cs)** —
  клон `RunningTriangleScanEngine`: резолв символа/таймфрейма из имени CSV,
  `EWParams` из инпутов страницы, по-барный прогон финдера, сбор
  `OnEnter` → `OnTakeProfit`/`OnStopLoss` в DTO с исходом (`TP`/`SL`),
  срез свечей под отрисовку. DTO переиспользованы без изменений
  (`TriangleScanResult` / `TriangleSetupDto`, 6 wave-точек 0–5); запрос —
  `DiagonalScanRequest { File, FromDate, ToDate, Period, MinSizePercent,
  BarsCount, TakeProfitRatio, RequireWave5Ratio, RequireWave4Ratio,
  RequireInitialDiagonal, TakeProfitAtRetrace, MinConvergence,
  RequireInsideWedge, MaxSpillAreaRatio, MinWave2Retrace }`.
- **`POST /api/diagonal/scan`** в [Program.cs](TradeKit.ReplayViewer/Program.cs) —
  по образцу `/api/rtriangle/scan` (имя файла нормализуется
  `Path.GetFileName`, чтобы запрос не вышел за `dataDir`).
- **[diagonal.html](TradeKit.ReplayViewer/wwwroot/diagonal.html) +
  [diagonal.js](TradeKit.ReplayViewer/wwwroot/js/diagonal.js)** — quiz-UI:
  зигзаг 0-1-2-3-4-5, трендлинии 1-3 и 2-4 (сходящийся клин), уровни
  W3/entry/TP/SL, маркеры волн; пользователь угадывает исход (TP/SL),
  статистика ответов. Параметры: Период (0=авто), Размер %, Баров,
  R:R, числовое поле «Сходимость» (0 по умолчанию), чекбокс «Бары в клине»
  с полем «Выброс», «Откат W2», «W5 ≥ 78%», «W4 ≥ 78%», «Заходность диагонали»
  и «TP 23.6%».
- Ссылка «◺ Диагонали» добавлена в навигацию index/impulse/triangle/rtriangle.

### 9.3. Критерии приёмки самотеста

- На полном прогоне нескольких пар (m15 и h1) финдер выдаёт **ненулевое, но
  осмысленное** число сетапов (ориентир по треугольникам: единицы–десятки на
  файл-историю, не сотни).
- Ни один выданный сигнал не нарушает инварианты §9.1.
- Визуальная проверка на quiz-странице: клин сжимается, перекрытие W4↔W1
  видно глазом, триггерная свеча пробивает V(3).
- Funnel-отчёт не показывает гейта, убивающего >70–80% кандидатов без
  структурной причины.

### 9.4. Фактические результаты первого прогона

> Все цифры §9.4 и §9.5 сняты **до** появления D-CONVERGE и D-INSIDE, то есть
> при `MinConvergence = −1` и `RequireInsideWedge = false`. Сравнение с
> включёнными фильтрами — §9.6 и §9.7.

Полный отчёт — [reports/diagonal_rejections.md](reports/diagonal_rejections.md).
По AUDUSD (полная история), `TakeProfitRatio = 1.5`, soft-фильтры выключены:

| файл | период зигзага | скелетов собрано | кандидатов в пуле | сигналов |
|---|---:|---:|---:|---:|
| `AUDUSD_m15` | 26 | 530 004 | 4 238 | **496** |
| `AUDUSD_h1` | 47 | 160 260 | 1 117 | **135** |

Гейта-убийцы нет: основной отсев приходится на чисто геометрические
правила (`w2BeyondStart` → `w3NoPenetration` → `w3NotContracting` →
`w4NotContracting`), что ожидаемо: перебор точек 0 порождает много заведомо
невалидных скелетов. Среди зарегистрированных кандидатов отсев распределён
ровно (`durationInsane` ≈ 44%, `tooFewBars` ≈ 35%, `noOverlap` ≈ 31%,
`notContained` ≈ 7%), время полного прогона — единицы секунд.

Сравнение режимов (`Diagonal_ModeComparison_Report`, `AUDUSD_h1`):

| `RequireWave5Ratio` | R:R | входов | win% | матожидание |
|---|---:|---:|---:|---:|
| false | 1.0 | 135 | 51.9% | +0.04R |
| false | 1.5 | 144 | 41.0% | +0.02R |
| false | 2.0 | 139 | 33.1% | −0.01R |
| false | 3.0 | 127 | 21.3% | −0.15R |
| true | 1.0 | 58 | 43.1% | −0.14R |
| true | 1.5 | 72 | 41.7% | +0.04R |
| true | **2.0** | 78 | 35.9% | **+0.08R** |
| true | 3.0 | 86 | 25.6% | +0.02R |

Выводы: геометрия работает (win% стабильно выше безыскового уровня
`1/(1+R)` на малых R), но края тонкие — без фильтра контекста это не готовая
торговая система, а база для тренажёра и дальнейшей фильтрации.
«Зрелая волна 5» окупается только на R ≥ 1.5.

Добавленный позже фильтр `RequireWave4Ratio` (D-W4-78) на той же выборке
ухудшает результат во всех комбинациях:

| `RequireWave5Ratio` | `RequireWave4Ratio` | R:R | входов | win% | матожидание |
|---|---|---:|---:|---:|---:|
| false | true | 1.0 | 41 | 41.5% | −0.17R |
| false | true | 1.5 | 55 | 34.5% | −0.14R |
| false | true | 2.0 | 59 | 25.4% | −0.24R |
| false | true | 3.0 | 61 | 13.1% | −0.48R |
| true | true | 1.0 | 23 | 30.4% | −0.39R |
| true | true | 1.5 | 35 | 37.1% | −0.07R |
| true | true | 2.0 | 39 | 30.8% | −0.08R |
| true | true | 3.0 | 43 | 18.6% | −0.26R |

Выборка режется в 2–3 раза, а win% падает сильнее, чем растёт качество:
на архиве ровное сжатие клина не коррелирует с качеством разворота.
Поэтому флаг остаётся выключенным по умолчанию — он полезен как
инструмент отбора «красивых» примеров для тренажёра, но не как торговый
фильтр. Проверять на других парах до окончательного вывода.

Фильтр `RequireInitialDiagonal` (D-INIT) — фактически срез «только
конечные диагонали» — на том же архиве оставляет ~43% входов и почти не
меняет края (цифры сняты до расширения гейта на всю диагональ, то есть
когда проверялось только движение `V(0) → V(1)`; новый вариант строго
жёстче и даёт не больше входов):

| `RequireWave5Ratio` | `RequireWave4Ratio` | R:R | входов | win% | матожидание |
|---|---|---:|---:|---:|---:|
| false | false | 1.0 | 58 | 53.4% | **+0.07R** |
| false | false | 1.5 | 64 | 40.6% | +0.02R |
| false | false | 2.0 | 65 | 29.2% | −0.12R |
| false | false | 3.0 | 64 | 18.8% | −0.25R |
| true | false | 1.0 | 30 | 46.7% | −0.07R |
| true | false | 1.5 | 38 | 44.7% | **+0.12R** |
| true | false | 2.0 | 40 | 32.5% | −0.03R |
| true | false | 3.0 | 43 | 20.9% | −0.16R |
| false | true | 1.0 | 19 | 52.6% | +0.05R |
| true | true | 1.5 | 17 | 41.2% | +0.03R |

На R:R = 1.0 win% чуть выше (53.4% против 51.9%), в связке с «зрелой
волной 5» на R:R = 1.5 даёт лучшее матожидание по всей сетке (+0.12R), но
выборка в 38 входов слишком мала, чтобы считать это подтверждением.
Вывод: «заходность» — не улучшение краёв и **не** классификатор типа
диагонали (её проходит и конечная диагональ в волне C плоскости, и часть
начальных, §5.2 п. 4), а фильтр **контекста старта**. Дефолт — выключено;
полезен для тренажёра, чтобы отбирать примеры с чистым разворотом перед
точкой 0.

### 9.5. Режим TP «23.6% диагонали» (D-TP-236)

`TakeProfitMode = DIAGONAL_RETRACE`, тот же AUDUSD h1. R:R здесь плавающий,
поэтому матожидание считается по фактическому R каждой сделки; `avgR` —
средний R на входе:

| `RequireWave5Ratio` | `RequireWave4Ratio` | `RequireInitialDiagonal` | входов | avgR | win% | матожидание |
|---|---|---|---:|---:|---:|---:|
| false | false | false | 100 | 0.62 | **64.0%** | **+0.01R** |
| true | false | false | 58 | 1.72 | 39.7% | −0.02R |
| false | true | false | 30 | 0.96 | 46.7% | −0.14R |
| false | false | true | 41 | 0.70 | 58.5% | −0.06R |
| false | true | true | 15 | 1.11 | 53.3% | +0.01R |
| true | true | true | 11 | 1.52 | 27.3% | −0.30R |

Наблюдения:

- Цель, привязанная к геометрии, даёт **сильно более высокий win rate**
  (64% против 51.9% при фиксированном R:R = 1.0), но и `avgR` всего 0.62 —
  итог по матожиданию тот же околонулевой (+0.01R). Это ровно тот же край,
  просто переупакованный в другую пару «частота × размер».
- Опция «зрелая волна 5» и режим 23.6% **работают в одну сторону**: чем
  ближе вход к потолку `|W3|`, тем короче стоп и тем дальше 23.6%-цель →
  `avgR` растёт с 0.62 до 1.72, а win rate падает до 39.7%. Пара
  «W5 ≥ 78% + TP 23.6%» — самый «естественный» режим по построению, но на
  архиве он не лучше фиксированного R:R.
- Входов меньше (100 против 135): часть сигналов отсекается новым гейтом
  `tpBehindEntry` — триггерная свеча закрылась уже за уровнем 23.6%.

### 9.6. Сходимость образующих (D-CONVERGE) — шкала и эффект

AUDUSD h1, D-INSIDE включён (0.005), прочие опции выключены. `MinConvergence`
= −1 означает «фильтр отключён».

| `MinConvergence` | R:R 1.0 | R:R 1.5 | R:R 2.0 | TP 23.6% |
|---|---:|---:|---:|---:|
| −1 (выкл.) | 107 вх., 57.9%, +0.16R | 111, 45.9%, +0.15R | 109, 38.5%, +0.16R | 85, 62.4%, +0.01R |
| **0** (деф.) | 102, 59.8%, **+0.20R** | 105, 47.6%, +0.19R | 103, 40.8%, +0.22R | 80, 63.8%, +0.02R |
| +0.5 | 70, 62.9%, **+0.26R** | 69, 53.6%, **+0.34R** | 69, 43.5%, +0.30R | 56, 67.9%, +0.04R |
| +1.0 | 44, 59.1%, +0.18R | 43, 55.8%, **+0.39R** | 44, 45.5%, **+0.36R** | 35, 60.0%, −0.09R |

В связке с «зрелой волной 5» (`MinConvergence = 0`): R:R 2.0 → 41.8% win,
**+0.25R** (55 входов); R:R 3.0 → 31.1%, **+0.25R** (61 вход).

Выводы:

- переход от «выключено» к «0» стоит ~5% входов и добавляет +0.04…+0.06R —
  расходящийся клин это не диагональ, а расширяющаяся структура или неудачно
  нарезанный тренд, ждать от него разворота с потолком `|W3|` нет оснований;
- **ужесточение работает как регулятор качества**: `+0.5` и `+1.0`
  поднимают матожидание до +0.34…+0.39R, но выборка падает вдвое-втрое
  (102 → 70 → 44 входа). На R:R 3.0 ужесточение уже вредит — цель уходит
  дальше, чем позволяет узкий клин;
- по умолчанию оставлено **0** (решение O-10): это ровно граница
  «параллельно», отбрасывающая только реально расходящиеся клинья, а
  дальнейшее ужесточение — осознанный выбор пользователя.

### 9.7. Калибровка порога выброса (D-INSIDE)

Research-тест `Diagonal_SpillThreshold_Report` гоняет три полные истории при
включённом D-CONVERGE и прочих опциях по умолчанию. `off` = фильтр выключен.

**AUDUSD h1** (матожидание, R:R 1.0 / 1.5 / 2.0):

| `MaxSpillAreaRatio` | входов (R:R 1.0) | win% | 1.0 | 1.5 | 2.0 |
|---|---:|---:|---:|---:|---:|
| off | 131 | 52.7% | +0.05R | +0.05R | +0.06R |
| 0.020 | 119 | 56.3% | +0.13R | +0.11R | +0.15R |
| 0.010 | 110 | 58.2% | +0.16R | +0.14R | +0.18R |
| **0.005** | 102 | 59.8% | **+0.20R** | **+0.19R** | **+0.22R** |
| 0.003 | 95 | 61.1% | +0.22R | +0.21R | +0.22R |
| 0.002 | 91 | 59.3% | +0.19R | +0.16R | +0.15R |
| 0.001 | 81 | 58.0% | +0.16R | +0.14R | +0.15R |

Кривая имеет **плато на 0.003–0.005** и заваливается ниже 0.002 (выборка
становится слишком тонкой). Пороги ≥ 0.075 не отсекают вообще ничего —
у подавляющего большинства кандидатов выброс меньше.

Проверка на других парах (R:R 1.0, `off` → `0.005`):

| пара | входов | win% | матожидание |
|---|---:|---:|---:|
| AUDUSD h1 | 131 → 102 | 52.7% → 59.8% | +0.05R → **+0.20R** |
| GBPUSD h1 | 142 → 108 | 50.7% → 52.8% | +0.01R → +0.06R |
| EURUSD h1 | 116 → 99 | 50.0% → 49.5% | 0.00R → −0.01R |

Честная картина: **сильно помогает на AUDUSD, умеренно на GBPUSD, нейтрально
на EURUSD, нигде не вредит.** Направление одинаковое на двух парах из трёх,
поэтому фильтр включён по умолчанию с `0.005` — берётся более мягкий край
плато, чтобы сохранить выборку (решение O-11). Стоимость — около 20–25%
входов.

### 9.8. Калибровка D-W3-PEN и D-TIME

`Diagonal_NzdCadM5_Case_Diagnostics` (Explicit, Research) разбирает одну
размеченную вручную диагональ — NZDCAD m5, 2026-08-09 21:25 → 2026-08-10 09:25.
Тест прогоняет каждое правило §4 по ручному скелету, печатает, какие ступени
лестницы вообще видят эти пивоты, и дампит gate, на котором живой finder убил
кандидата. Это единственный способ отличить «модель не собралась» от «модель
собралась и её зарезал фильтр».

Диагноз оказался неожиданным: скелет **собирался правильно** на ступенях
period = 9…10, все правила Эллиотта проходили, а убивали его **два порога,
которые вообще не являются правилами Эллиотта**:

| правило | было | факт на разметке | вердикт |
|---|---:|---:|---|
| D-W3-PEN | 0.05 | пробой **4.87%** \|W1\| | FAIL на 0.13 пункта |
| D-TIME | 4.0 | W3/W1 = 35/7 = **5.00** | FAIL |

То есть валидный сжимающийся клин отбрасывался буквально на границе. Причина
системная: чем плотнее клин, тем **меньше** пробой волны 3 за волну 1 — порог
5% штрафует ровно ту геометрию, которую мы ищем; а волна 3 диагонали почти
всегда длиннее волны 1 по времени в разы, поскольку в неё вложена коррекция.

Оба порога вынесены в параметры и переоткалиброваны свипом
`Diagonal_PenetrationAndDurationThresholds_Report` (R:R 1.5, 4 набора данных,
`pen ∈ {0, 0.01, 0.03, 0.05, 0.1} × dur ∈ {4, 6, 8, 12, ∞}`):

| набор | 0.05 / 4.0 (было) | **0.03 / 8.0 (стало)** |
|---|---|---|
| AUDUSD h1 | 85 входов, 48.2%, +0.21R | 119 входов, 47.1%, +0.18R |
| AUDUSD m15 | 301, 42.2%, +0.05R | 463, 40.6%, +0.02R |
| EURUSD h1 | 95, 40.0%, 0.00R | 122, 39.3%, −0.02R |
| GBPUSD h1 | 74, 35.6%, −0.11R | 125, 37.9%, −0.05R |

Разброс матожидания по всей решётке — ±0.05R, то есть **шум**: ни один из 25
сочетаний не выигрывает статистически. Зато выборка растёт на 40–65%, а
пропущенная вручную размеченная диагональ находится. Поэтому берутся
`MinWave3Penetration = 0.03` и `MaxWaveDurationRatio = 8.0` — минимальное
ослабление, при котором кейс проходит.

Дополнительно D-TIME починен по смыслу: сравнивались **соседние** волны
(W2 против W1), теперь — **однохарактерные** (W3 против W1, W4 против W2).
Коррекция и импульс в диагонали различаются по длительности принципиально,
их сравнение друг с другом не несёт информации.

Остаточное ограничение: второй вариант разметки той же модели
(V(3) = 07:45, V(4) = 08:20, V(5) = 09:25) даёт W4/W2 = 7/61 = 8.71 и всё ещё
не проходит D-TIME при 8.0. Это осознанный размен: чтобы пропустить его,
порог пришлось бы поднимать до ~9, а первый вариант той же диагонали
находится и торгуется, поэтому сигнал не теряется.

### 9.9. Пропущенная диагональ EURAUD m5: жадное слияние подволн не режет сложную волну 2

`Diagonal_EurAudM5_Case_Diagnostics` (Explicit, Research) — тот же разбор, что
§9.8, но для EURAUD m5 (`EURAUD_m5_2026-07-09…2026-08-10.csv`), восходящий клин
с **волной 2 через выходные**:

| точка | время | экстремум | бар |
|---|---|---:|---:|
| V(0) | 07-31 13:40 | 1.63402 low | 4683 |
| V(1) | 07-31 14:10 | 1.64036 high | 4689 |
| V(2) | 08-02 21:15 | 1.63692 low | 4774 |
| V(3) | 08-02 22:00 | 1.64066 high | 4783 |
| V(4) | 08-03 00:10 | 1.63795 low | 4809 |
| V(5) | 08-03 02:30 | 1.64088 high | 4837 |

|W1|=0.00634, |W2|=0.00344, |W3|=0.00374, |W4|=0.00271, |W5|=0.00293.
Все правила §4 по ручному скелету **проходят**: D-W3-PEN 4.73% ≥ 3%,
D-CONTRACT-3/4, D-OVERLAP, D-W4-2, D-W5-BREAK, D-W5-CAP, D-CONVERGE +1.127,
D-INSIDE 0.000, D-TIME (W3/W1=1.50, W4/W2=3.27) ≤ 8. Размер и число баров тоже
в норме. То есть геометрически это валидный сжимающийся клин.

Тем не менее **никакая комбинация доступных параметров его не находит**
(свип `conv ∈ {−1,0} × spill ∈ {0.005,0.05,1} × pen ∈ {0,0.03}` — все «not
found»). Живой finder в этом окне выдаёт сигнал, но с точкой 0 = **08-02 21:15**
(это V(2) пользователя) — то есть находит **внутренний** клин воскресенье–понедельник,
а не внешний с заходом в пятницу. Кандидаты же с точкой 0 = 07-31 13:40 умирают
на `w3NoPenetration` / `w3NotContracting`: волна 3 у них вырезана неверно.

**Корень — не порог, а жадное слияние подволн `ExtendWave`.** Волна 2
(14:10 → 21:15) — сложная многозвенная коррекция через выходные: первое колено
14:10 → 14:55 (−0.00305), затем откат 14:55 → 15:55 (+0.00263). Правила
Эллиотта такую волну 2 допускают, но жадный алгоритм обрывает волну, как только
встречный ход превышает `WAVE_PULLBACK_TOL` от **накопленной** амплитуды.
Откат 0.00263 — это **86%** от 0.00305, поэтому волна 2 режется уже в 14:55,
не доходя до 21:15, и дальше строится маленький пятничный клин.

Ключевое: **ни одно одиночное значение допуска не режет этот скелет**, потому
что требования к волнам 1 и 2 взаимоисключающи:

| волна | условие на допуск `tol` | почему |
|---|---|---|
| W1 кончается в 14:10 | `tol ≤ 0.543` | обрывающий откат до воскресного минимума 21:15 = 0.00344 = 54.3% амплитуды 0.00634 (а пятничный откат 48.1% ещё не должен обрывать) |
| W2 доходит до 21:15 | `tol ≥ 0.862` | надо поглотить пятничный откат 0.00263 = 86.2% от текущей амплитуды 0.00305 |

Интервал `(0.481, 0.543] ∩ [0.862, ∞) = ∅`. Подтверждено экспериментально: свип
`wavePullbackTol ∈ {0.30 … 1.50}` при **выключенных всех пороговых gate**
(`conv=−1`, `spill=1.0`, `pen=0`) — все «not found». Значит, дело именно в
сегментации, а не в каком-либо фильтре.

Вывод: это **не баг кода и не настраиваемый параметр**, а структурное
ограничение одно-допускового жадного слияния. Оно не может вырезать волну, у
которой **внутренний** встречный ход глубже допуска, но сама волна по правилам
Эллиотта едина (типично — глубокая коррекция-волна 2, особенно через разрыв
сессии/выходные). Допуск вынесен в параметр конструктора `wavePullbackTol`
(по умолчанию **0.5**, поведение сохранено) — это позволяет экспериментировать,
но конкретный кейс не лечится никаким его значением.

Направления исправления:
- **(A) кросс-масштабная сборка — РЕАЛИЗОВАНО** (см. ниже): скелет 0-1-2-3-4
  собирается с волной 2 с **грубой** ступени лестницы, где откат 86% уже
  поглощается; аналогично §7.6 RunningTriangle;
- **(B) swing/BOS-экстремумы** — пивот подтверждается пробоем последнего
  значимого свинга, а не процентом разворота: пятничный откат не ломает свинг,
  волна 2 остаётся целой;
- **(C) look-ahead-слияние** — отклонено ранее (пересливает волну B у треугольников).

**Результат варианта (A).** В `TryAssembleAt` добавлен фолбэк
`TryCrossScaleWave2`: когда одно-масштабное жадное слияние обрывает волну 2
(`i4 != p4Idx`), волны 0-1 остаются на рунге-событии, конец волны 2 ищется на
каждой **более грубой** ступени (где откат 86% поглощается), а волны 3-4
вырезаются заново на любой ступени, резолвящей волну 4 как свежий пивот.
Кейс найден на **параметрах по умолчанию**: `V(0) 07-31 13:40 → V(5) 08-03 02:25`,
вход 1.64081, SL 1.64171, TP 1.63992. Свип `conv/spill/pen` — все FOUND; свип
`wavePullbackTol` — FOUND при `tol ∈ {0.30, 0.40, 0.50}` (при ≥0.60 и
одно-масштабный и кросс-масштабный путь пересливает скелет иначе). Регресс-тест
`Diagonal_EurAudM5_WeekendWave2_FoundByCrossScale` (не Explicit) фиксирует,
что сигнал с точкой 0 = 07-31 13:40 и концом волны 2 = 08-02 21:15 выдаётся.

### 9.10. Пропущенная диагональ GBPNZD m5: двойная вершина в конце волны 1

Кейс: GBPNZD m5, разметка `V(0) 07-28 21:25 (2.29240) → V(1) 07-29 03:05
(2.30095) → V(2) 06:00 (2.29577) → V(3) 08:45 (2.30138) → V(4) 11:25
(2.29897) → V(5) 12:20 (2.30197)`. Все 14 гейтов §4 проходят на ручном
скелете (D-W3-PEN 5.03%, D-W4-38 0.00241 ≥ 0.00214, D-TIME-24 32 < 35,
D-CONVERGE 1.642, D-INSIDE 0.00202, D-TIME 2.06/1.09) — диагональ валидна,
но детектор её не находил.

**Диагностика** (тест `Diagonal_GbpNzdM5_Case_Diagnostics`, Explicit):
проблема не в параметрах. Ключевая особенность — **двойная вершина**: бары
02:00 и 03:05 имеют одинаковый high **точно** 2.30095. Из-за неё:

1. На ступенях 9/10 (единственных, резолвящих волну 4 на 11:25 — паттерн
   пивотов `######`) жадное слияние `ExtendWave` со строгим `>` оставляет
   экстремумом волны 1 **первое** касание 02:00 (равное второе не побеждает),
   и вся нарезка смещается: W2 = 02:00→02:45, W3 = 02:45→03:05,
   W4 = 03:05→06:00 → конец волны 4 ≠ выбранному пивоту, сборка умирает;
2. кросс-масштабный фолбэк якорит волну 1 на фантомном 02:00, но ступени 11+
   его не резолвят (`###..#`) → `FindPivot` = −1 → тоже тупик;
3. ступени 11+ честно режут волну 1 до 03:05, но зато сливают волну 3
   06:00→12:20 — детектор выдавал лишь «мёрдж»-сигнал
   `V(0) 21:25 | V(1) 03:05 | V(2) 06:00 | V(3) 12:20 | V(4) 13:35 | V(5) 14:40`,
   причём идентичный при любом значении свипов `conv/spill/pen` и
   `wavePullbackTol ∈ {0.30 … 0.60}` (при ≥ 0.70 не находится вообще).

**Вывод**: ни одно значение `wavePullbackTol` не лечит кейс — это снова
структурное ограничение, теперь на стороне волны 1: конец волны, который
рунг-событие выбирает по первому касанию двойной вершины, не существует на
грубой ступени, где волны 3-4 режутся правильно.

**Исправление (вариант A, обобщение §9.9).** В `TryCrossScaleWave2` добавлен
фолбэк: если грубая ступень не резолвит конец волны 1 рунга-события
(`FindPivot(cpiv, p1) < 0`), волна 1 **вырезается заново от точки 0** на этой
ступени (`ExtendWave(cpiv, c0, …, wantVHigh)`), и найденный конец `v1`
используется в скелете вместо `p1`. Остальной путь (волна 2 на грубой,
волны 3-4 на средней ступени) не меняется; гейты §4 отсеивают мусорные
скелеты, если перерезка даёт не диагональ.

На кейсе: ступени 9/10 обрываются на баре 12:05 → фолбэк; на ступени 11
`FindPivot(02:00)` не найден → перерезка волны 1 от 21:25 → конец 03:05
(откат 01:35 0.00303 < допуска поглощается, откат 06:00 0.00518 обрывает),
волна 2 → 06:00; средняя ступень 9/10: конец волны 3 ограничен пивотом
11:25 (`endLimit` не даёт слить до 12:20) → 08:45, волна 4 = 11:25 — скелет
`21:25 | 03:05 | 06:00 | 08:45 | 11:25` проходит все гейты.

**Тайминг**: пивот 11:25 подтверждается на баре 12:05 (deviation 0.09% ≈
0.00207, нужен high > 2.30104; 12:05 H = 2.30138) → регистрация ~12:05,
волна 5 добивается с 11:26; триггер 12:15 (H = 2.30149 > V(3) 2.30138),
вход = close 12:15 = 2.30121, SELL, SL = 2.29897 + 0.00561 = 2.30458,
TP = 2.29784. Грубый «мёрдж»-кандидат регистрируется позже (~14:30) и
блокируется `IsInSetup` — более тонкий сигнал выигрывает.

Регресс-тест `Diagonal_GbpNzdM5_DoubleTopWave1_FoundByCrossScale`
(не Explicit) фиксирует, что сигнал с точкой 0 = 07-28 21:25 выдаётся со
скелетом `03:05 / 06:00 / 08:45 / 11:25`. Weekend-тест §9.9 продолжает
проходить: фолбэк срабатывает только когда `FindPivot(p1)` не найден, старый
путь байт-в-байт сохранён.

### 9.11. Пропущенная диагональ CADJPY m5: волна 1 перелита мелкой волной 2

Кейс: CADJPY m5, разметка `V(0) 08-19 12:35 (114.084) → V(1) 14:05 (114.686)
→ V(2) 14:35 (114.480) → V(3) 15:10 (114.726) → V(4) 15:20 (114.607) →
V(5) 15:25 (114.736)`. Все 15 гейтов §4 на ручном скелете проходят
(D-W3-PEN 6.64%, D-W4-38 0.119 ≥ 0.094, D-W4-24 0.383, D-CONVERGE 1.322,
D-INSIDE 0.00216), ступени лестницы 9/10 резолвят **ровно** эти шесть пивотов.
Тем не менее детектор ставил точку 0 на **13:10** — на один пивот правее.

**Корень — зеркало §9.9: волна 1 не режется, а «перели­вается».** Жадное
слияние обрывает волну, когда встречный ход превышает `WavePullbackTol` от
**накопленной** амплитуды. Здесь волна 1 — пролив 12:35 (|W1| = 0.602), а
волна 2 откатывает всего 0.206 = **34.2%**, то есть **меньше** допуска 0.5 →
волны 2-3-4 поглощаются волной 1, скелет с точкой 0 = 12:35 не собирается.
Со следующего пивота 13:10 |W1| = 0.285, и та же волна 2 = 72% > 50% —
волна 1 корректно кончается на 14:05. Свип подтверждает порог ровно
`|W2|/|W1| = 0.342`: при `wavePullbackTol ≤ 0.34` находится разметка
пользователя, при ≥ 0.40 — «правая».

**Исправление (вариант C).** В `TryAssembleAt` нарезка волн 2-4 вынесена в
`TryCarveWaves234`, и добавлен фолбэк `TryShorterWave1`: если и одномасштабная
сборка, и кросс-масштабный фолбэк ничего не зарегистрировали, перебираются
ближайшие **более ранние** пивоты в направлении диагонали как конец волны 1
(не более `MAX_WAVE1_ALTERNATIVES` = 4). Старые пути сохранены байт-в-байт:
фолбэк включается только когда пул кандидатов не вырос.

**Плюс детерминизм выбора.** Внешний и внутренний клин пробивают `V(3)` на
одной свече, и раньше входил тот, что первым попался в обходе `Dictionary`
(порядок зависит от хеша и удалений). Теперь сработавшие на баре кандидаты
собираются в буфер и сортируются по **самой ранней точке 0** — выигрывает
внешний клин, а результат воспроизводим между прогонами.

Регресс-тест `Diagonal_CadJpyM5_ShallowWave2_FoundByShorterWave1`. Побочно:
в режиме TP «23.6% диагонали» неверная точка 0 = 13:10 давала уровень 114.657,
задетый уже внутри триггерной свечи (`tpSlHit`), — с правильной точкой 0
сетап выдаётся (TP 114.582).

### 9.12. NZDJPY m5: два «почти клина» и пороги D-W4-24 / D-TIME-24

Два кейса на NZDJPY m5, оба валидны на глаз, но отбраковываются гейтами §4 —
причём **на волосок**:

| кейс | скелет | что падает |
|---|---|---|
| 1 | `08-19 12:35 → 14:05 → 14:20 → 15:35 → 15:50 → 16:05` | D-W4-24 = **0.206**, D-TIME-24 (bars W4 = W2 = 3), D-INSIDE = **0.00915** |
| 2 | `08-19 21:25 → 08-20 05:30 → 07:05 → 14:40 → 15:05 → 19:55` | только D-W4-24 = **0.226** |

Порог D-W4-24 (0.236) в обоих случаях недобирается на единицы тысячных, поэтому
он вынесен из константы в параметр `MinWave4Wave2Level`, а D-TIME-24 — в флаг
`RequireWave4Shorter` (дефолты прежние, поведение не меняется).

Результат свипа (`Diagonal_NzdJpyM5_Case_Diagnostics`):

- **кейс 2 лечится порогом**: при `MinWave4Wave2Level ≤ 0.20` детектор выдаёт
  `V(0) 21:55 | V(1) 05:30 | V(2) 07:05 | V(3) 14:40 | V(4) 15:05 | V(5) 19:45`,
  вход 94.555 (точка 0 — ближайший пивот, волны 1-4 совпадают с разметкой);
- **кейс 1 не лечится ничем из 16 комбинаций** `w4Level × w4Shorter × spill`:
  скелет не вырезается вообще. Внутри его волны 3 сидит откат **93.9%**
  (14:40), то есть это снова ограничение сегментации (§9.9), а не порогов;
- **выключать D-TIME-24 опасно**: без него в оба окна пролезает многодневный
  клин `08-03 → 08-19` и занимает слот сетапа раньше локального.

Практический вывод: `MinWave4Wave2Level` 0.20-0.236 — рабочий регулятор
чувствительности, `RequireWave4Shorter` лучше оставлять включённым.

### 9.13. EURCHF m5: волна 4 на изолированном принте

Кейс: EURCHF m5, `data/EURCHF_m5_2026-08-14T07-20-00_2026-08-21T20-55-00.csv`.
Финдер выдавал диагональ вверх
`V(0) 08-19 21:10 (0.93033) → V(1) 08-20 07:40 (0.93450) → V(2) 08-20 10:35
(0.93086) → V(3) 08-20 19:05 (0.93502) → V(4) 08-20 21:00 (0.93288) →
V(5) 08-21 03:30 (0.93503)` — SELL. Проблема — точка 4: бар 21:00 это одиночный
принт, а не конец коррекции.

Числа по бару 21:00: OHLC = 0.93288 (нулевой диапазон), предыдущий бар
[0.93469, 0.93475], следующий [0.93438, 0.93473] — диапазон принта не
пересекается ни с одним из соседей (**двусторонний гэп** ~18 и ~15 пунктов);
отрыв `D = 0.00150` ≈ **14 медианных диапазонов** предшествующих 50 баров
(медиана 0.00011); возврат полный и **в первый же бар** (хай следующего бара
0.93473 выше обеих границ гэпа); к уровню 0.93288 цена не вернулась ни разу до
конца файла (минимальный лоу после — 0.93425). Все три условия §4.4 выполнены с
многократным запасом; сегмент подтверждается к бару 22:00 (окно 12 баров),
задолго до пробоя `V(3)` в 03:30.

С фильтром пивот на баре 21:00 выбрасывается из хвоста, скелет на принте не
собирается и сигнал не выдаётся. Регресс-тест —
`Diagonal_EurChfM5_IsolatedPrintWave4_Excluded`: подтверждение сегмента и
отсутствие сигналов, использующих принт как любую из точек волны.

---

## 10. cTrader: индикатор и бот — реализовано

Обвязка сделана по образцу Harmonic (`HarmonicIndicator` +
`HarmonicSignalerBot`):

**В `TradeKit.Core/ElliottWave/`:**

- [DiagonalParams.cs](TradeKit.Core/ElliottWave/DiagonalParams.cs) — record со
  всеми диагональ-специфичными параметрами (§8) поверх `EWParams`;
- [DiagonalBaseAlgoRobot.cs](TradeKit.Core/ElliottWave/DiagonalBaseAlgoRobot.cs) —
  `BaseAlgoRobot<DiagonalSetupFinder, ElliottWaveSignalEventArgs>`. Отдельный
  класс, а не `ElliottWaveBaseAlgoRobot`: тот жёстко привязан к
  `ImpulseSetupFinder`/`ImpulseSignalEventArgs` и лезет в `Model.Wave0`, чего у
  диагонали нет — весь скелет 0-1-2-3-4-5 лежит прямо в `WavePoints`.
  Рисует TP/SL, зигзаг и **обе образующие** клина.

**В `TradeKit.cTrader/Diagonal/`** (все три файла добавлены в
`<Compile Include>` — у проекта `EnableDefaultItems=False`):

- `DiagonalFinderBaseIndicator.cs` — `BaseIndicator<DiagonalSetupFinder,
  ElliottWaveSignalEventArgs>`; рисует зигзаг 0-1-2-3-4-5, образующие 1-3 и
  2-4 и прямоугольники TP/SL; события §6.4 (`OnBreakeven`, `OnManualClose`)
  пишутся в лог на закрытии свечи;
- `DiagonalCTraderBaseRobot.cs` — те же `[Parameter]`, `GetEWParams()` +
  `GetDiagonalParams()`;
- `DiagonalSignalerAlgoRobot.cs` — `CreateBarsProvider` / `CreateSetupFinder`.

**Проекты** (добавлены в `TradeKit.sln`):

- [DiagonalIndicator](DiagonalIndicator/DiagonalIndicator.cs) —
  `[Indicator(IsOverlay = true, …)]`;
- [DiagonalSignalerBot](DiagonalSignalerBot/DiagonalSignalerBot.cs) —
  `[Robot(AccessRights = AccessRights.FullAccess)]`.

Ядро (`DiagonalSetupFinder`) о cTrader по-прежнему ничего не знает.

---

## 11. Решённые вопросы

- **O-1. Только сжимающиеся диагонали** (`DIAGONAL_CONTRACTING_*`).
  Expanding исключены: редки, стоп от |W3| для них не работает.
- **O-2. Перекрытие W4↔W1 — hard-правило** (D-OVERLAP). Без перекрытия
  кандидат — импульс, а не диагональ.
- **O-3. Вход**: market на закрытии свечи, пробившей `V(3)` (пробой
  хвостом/хай-лоу достаточен), против направления диагонали.
  `SL = V(4) ± |W3|`, `TP = entry ∓ R·(SL − entry)`.
- **O-4. `TakeProfitRatio` по умолчанию = 1.0** (R:R = 1).
- **O-5. Initial/ending не различаем**: геометрия и торговая механика
  одинаковы, надёжной классификации на уровне зигзаг-пивотов нет. Тип можно
  выводить в комментарий сигнала как метку позже, если появится контекст
  старшей структуры.
- **O-6. `IsInitialMovement` — опция, а не обязательное правило.** Точка 0
  нужна **только** для измерения волны 1 и проверки сомасштабности волн 1 и 3
  (§5.1). Требовать «свежести» точки 0 по умолчанию нельзя — начальные
  диагонали стартуют прямо из конца предыдущего движения. Флаг
  `RequireInitialDiagonal` (D-INIT) добавлен как **контекстный** фильтр
  (старт со свежего разворота), **не** как классификатор «начальная/конечная»:
  его проходит и конечная диагональ в волне 5 импульса, и конечная в волне C
  плоскости, и часть начальных (§5.2, п. 4).
- **O-16. Гейт «заходности» распространён на всю диагональ.** Изначально
  проверялось только движение `V(0) → V(1)`, но «заходной» должна быть модель
  целиком: если волна 5 уже вынесла цену за предыдущее контр-движение, старт
  волны 1 внутри него ничего не говорит о контексте. Поэтому тест берёт
  конечную точку `W5` — экстремум волны 5 **на баре сигнала**, — а значит
  выполняется не при сборке скелета, а в момент сигнала (там он и известен).
  Условие строго жёстче прежнего: чем дальше конечная точка, тем труднее найти
  перед точкой 0 бар, зашедший ещё дальше, поэтому новых входов гейт добавить
  не может. Флаг переименован в `RequireInitialDiagonal`, правило — в D-INIT,
  счётчик воронки — в `notInitialDiagonal`.
- **O-17. Откат волны 2 (D-W2-RET) — числовой параметр `MinWave2Retrace`,
  по умолчанию 0 (выключено).** Симметрично D-W4-38 (откат волны 4 от
  волны 3), но для первой коррекции и без жёсткого дефолта: минимальной глубины
  волны 2 в правилах Эллиотта нет (есть лишь запрет уйти за точку 0, D-W2),
  поэтому это регулятор качества. Полезен ещё и как защита от разметки с
  «перелитой» волной 1 (§9.11): там волна 2 откатывала 34% волны 1.
  Гейт — `w2RetraceTooShallow`.
- **O-7. Soft-фильтр фибо `W3/W1` остаётся выключенным** (§4). Funnel §9.4
  показал, что гейта-убийцы нет и без него; включать только если появится
  замеренный выигрыш по матожиданию.
- **O-8. Референсный пример** (§9.1) подбираем после визуального просмотра
  quiz-страницы; кандидатов предлагает сам финдер (89 сетапов на AUDUSD h1
  за 2022 год при R:R = 1.5).
- **O-9. Режим TP «23.6% диагонали» — опция, дефолт `RISK_RATIO`.**
  Геометрическая цель осмысленнее фиксированного R:R (минимальная коррекция
  измеряется от самого движения), но на архиве она лишь переупаковывает тот
  же край: win rate 64% при `avgR` 0.62 против 51.9% при R = 1.0, матожидание
  в обоих случаях ≈ 0 (§9.5). Оставляем выбор пользователю.
- **O-10. Сходимость образующих (D-CONVERGE) — числовой параметр
  `MinConvergence`, по умолчанию 0.** Изначальное утверждение «сходимость
  следует из сжатия» ошибочно: наклон образующей зависит и от времени,
  поэтому при `|W4| < |W2|` клин всё равно может расходиться (§4.2).
  Вместо флага «сходится/нет» введена мера `w(t1)/w(t4) − 1`: 0 —
  параллельно, +1 — вдвое уже, −1 — предел расхождения. Дефолт 0
  соответствует прежнему поведению «отбрасываем только расходящиеся»;
  ужесточение до +0.5…+1 — регулятор качества (§9.6).
- **O-12. Переоценка уровня 23.6% после входа — опция `RetraceAction`,
  по умолчанию `NONE`.** Волна 5 не закончена в момент сигнала, поэтому
  зафиксированный уровень 23.6% — оценка по неполным данным (§6.4). Вместо
  того чтобы двигать TP вслед за экстремумом (это превратило бы сетап в
  трейлинг), пересчитанный уровень используется как **однократный триггер**
  управления позицией: безубыток, фиксация половины, обе меры сразу либо полный
  выход. Новое событие не вводится — задействованы штатные `OnBreakeven`
  (с добавленным флагом `MoveStopToEntry`) и `OnManualClose`. Дефолт `NONE`
  сохраняет прежнее поведение и оставляет режимы сравнимыми по статистике.
- **O-13. Невыгодный R:R в режиме 23.6% — ждём, а не пропускаем: параметр
  `MinRiskRewardRatio`, по умолчанию 0 (выключен).** Геометрическая цель даёт
  плавающий R:R (§6.3), и на раннем пробое `V(3)` он может быть заметно ниже 1.
  Отбрасывать такую диагональ жалко — модель-то валидна, плох только момент
  входа, — поэтому кандидат паркуется и перепроверяется на каждой закрытой
  свече: вход уходит вглубь волны 5 (ближе к стопу), а цель пересчитывается от
  свежего экстремума волны 5 (§6.5). Реакция по закрытию свечи, а не по тику:
  позиции ещё нет, а вход по закрытию — базовая механика финдера; перевод на
  тики (как в §6.4) оставлен на будущее. Дефолт 0 сохраняет прежнее поведение.
- **O-14. Изолированные принты исключаются из анализа (§4.4).** Одиночный принт —
  бар, чей диапазон не пересекается с диапазонами обоих соседей и гэп которого
  быстро возвращён, — это цена, которую рынок не торговал; строить на нём волну
  значит строить модель на плохом исполнении. Компонент `IsolatedPrintFilter`
  подтверждает сегменты с задержкой `RetraceWindowBars` баров, после чего финдер
  перестаёт потреблять такие бары (пивоты, волна 5, D-CONTAIN, D-INSIDE). Зигзаг
  не трогается и остаётся детерминированным. Сессионные/выходные гэпы критерий
  не ловит — гэп односторонний, кросс-скейл механика §7.2 продолжает работать.
- **O-15. D-TIME-12 — опциональное «время сжимается вместе с ценой» для волн 1–2.
  Флаг `RequireWave2Shorter`, по умолчанию выключен.** Требование `bars(W2) <
  bars(W1)` продолжает цепочку D-TIME-24 на более раннюю пару волн. В отличие от
  D-TIME-24 (там откат волны 4 длиннее волны 2 — явный признак дрейфа), для пары
  1–2 контракция времени не обязательна в определении диагонали, поэтому правило
  добавлено как настройка качества, а дефолт оставлен выключенным — поведение без
  флага не меняется. Гейт — `w2TimeNotContracting`, стоит сразу после D-W2.
- **O-11. Бары внутри клина (D-INSIDE) — опция, включённая по умолчанию,
  порог 0.005.** Критерий — доля площади клина, проведённая снаружи
  образующих (§4.3): она по построению прощает одиночные хвосты и ловит
  долгие выходы. Порог откалиброван по плато кривой матожидания (§9.7) и
  проверен на трёх парах: заметный выигрыш на AUDUSD и GBPUSD, нейтрально
  на EURUSD, вреда нигде. `MaxSpillAreaRatio` вынесен в параметры —
  как порог, чувствительный к рынку, он подлежит перепроверке при
  расширении выборки.
