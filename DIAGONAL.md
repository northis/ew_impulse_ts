# DIAGONAL — Диагональ (Leading / Ending Diagonal) как торговый сетап

> Проектный документ. Фиксирует видение отдельного детектора **сжимающихся
> диагоналей** (`DIAGONAL_CONTRACTING_INITIAL` / `DIAGONAL_CONTRACTING_ENDING`)
> для торговли разворота/коррекции, которые всегда следуют за диагональю.
>
> Статус: **дизайн, до реализации.** Реализацию начинаем после ревью этого
> документа. Аналог по подходу — [EW_R_TRIANGLE.md](EW_R_TRIANGLE.md)
> (running-треугольник): там же зафиксированы уроки, которые здесь
> переиспользуются (лестница периодов, cross-scale сборка, диагностика гейтов).

---

## 1. Мотивация и суть

Диагональ — терминальная модель: после неё **всегда** возникает движение в
противоположную сторону:

- **начальная (leading) диагональ** — волна 1 или A → за ней идёт **глубокая
  коррекция** (волна 2 или B), обычно 50–95% диагонали;
- **конечная (ending) диагональ** — волна 5 или C → за ней идёт **новый тренд**
  (разворот всей структуры).

Оба случая торгуются одинаково — **против направления диагонали**, поэтому
детектору не нужно знать, какая именно это диагональ (решение O-5, §11).

Ключевые свойства сетапа:

1. **Сигнал до завершения модели.** Мы не ждём подтверждения конца волны 5 —
   входим на закрытии свечи, **пробившей конец волны 3** после сформированной
   волны 4. Пробой волны 3 гарантирует, что усечения (truncation) исключены:
   волна 5 уже сделала новый экстремум.
2. **Стоп самомасштабируемый и точный**: в сжимающейся диагонали `|W5| < |W3|`
   (hard-правило), значит цена не может уйти дальше, чем `V(4) + длина W3`.
   Стоп ставится ровно на теоретический максимум волны 5 — его пробой
   *одновременно* и стоп, и инвалидация модели.
3. **TP через множитель R:R** от стопа (по умолчанию 1.0) — потому что цель
   после диагонали разная (коррекция vs новый тренд) и фиксированного
   структурного уровня, как у треугольника (точка 0 / волна B), здесь нет.

Расширяющиеся (expanding) диагонали **не рассматриваются** (решение O-1):
они крайне редки (коэффициенты 0.005–0.01 в [EW_RULES.md](EW_RULES.md) §8),
и для них не работает логика стопа от длины волны 3 (`|W5| > |W3|`).

---

## 2. Соотнесение с существующим кодом

| Аспект | Что уже есть | Что делаем в `DiagonalSetupFinder` |
|---|---|---|
| Правила диагонали | [`CheckDiagonalContracting`](TradeKit.Core/AlgoBase/ElliottWaveExactMarkupV2.PriceRules.cs) (v2): W2 не за начало W1, пробой W3 за W1 (`MIN_DIAGONAL_PENETRATION` = 5% |W1|), сжатие `|W3|<|W1|`, `|W4|<|W2|`, `|W5|<|W3|` | Явные инкрементальные правила §4 (без зависимости от общего markup). Отличие: **добавляем hard-правило перекрытия W4↔W1** (в v2 его нет) и **запрещаем усечение** (в v2 усечение разрешено для ENDING) |
| Зигзаг | [`DeviationExtremumFinder`](TradeKit.Core/Indicators/DeviationExtremumFinder.cs) — неизменяемое прошлое, основа инкрементальности | Тот же |
| Автопериод | [`AutoPeriodEstimator`](TradeKit.Core/AlgoBase/AutoPeriodEstimator.cs) (`Period == 0` → авто) | Тот же; базой берём импульсную оценку (диагональ — моторная модель), см. §7 |
| Мультимасштаб | Лестница `LADDER_RATIOS` из `RunningTriangleSetupFinder` (шаг ∜φ…φ) | Та же идея; дубли схлопываются по точке 0 + TP/SL-подписи |
| Cross-scale | `TryCrossScale` в `RunningTriangleSetupFinder`: старшие волны с крупного рунга, младшие — по **сырым барам** | Обязателен: триггер (пробой W3 закрытой свечой) — это событие **бара**, а не пивота зигзага, поэтому волна 5 всегда ведётся по сырым барам (§6.2) |
| «Заходность» | [`IsInitialMovement`](TradeKit.Core/Common/SingleSetupFinder.cs) | **Опционально, по умолчанию выключено** (решение O-6, §5.2): диагональ бывает и начальной, и конечной, поэтому требовать «свежести» точки 0 по умолчанию нельзя — но флаг `RequireInitialMovement` даёт включить гейт как контекстный фильтр и сравнить статистику |
| Диагностика | `Diag`/`Bump`/`OnGate` из `RunningTriangleSetupFinder` | Переносим (funnel-диагностика гейтов, §9) |
| Сигнал/события | `ElliottWaveSignalEventArgs`, `OnEnterInvoke`/`OnTakeProfitInvoke`/`OnStopLossInvoke`, `PERCENT_ALLOWANCE_TP/SL`, округление до `Symbol.Digits` | Как в `ImpulseSetupFinder`/`RunningTriangleSetupFinder` |

**Урок из running-треугольника, критичный здесь** (EW_R_TRIANGLE.md §7.6):
список пивотов `DeviationExtremumFinder` зависит от истории воспроизведения —
мелкие внутриволновые экстремумы поглощаются `MoveExtremum` и транзиентно
исчезают. Поэтому пивотам доверяем только «скелет» (волны 1–4 на своём рунге),
а всё, что происходит **внутри текущей волны 5** (пробой V(3), длина 78–100%),
меряем по сырым барам — они детерминированы.

---

## 3. Геометрия и обозначения

Точки (по возрастанию времени): **0, 1, 2, 3, 4, 5** — концы волн. Волна `Wk` —
движение от точки `k−1` к точке `k`; каждая волна может состоять из **одного или
нескольких сегментов зигзага** (подволн). `V(x)` — цена точки x.

Пример для **бычьей диагонали** (`isUp` = диагональ вверх → сигнал **SELL**;
медвежий случай зеркален):

```
                                          ┄┄┄┄┄┄┄ SL = V(4) + |W3| (теор. максимум W5)
                                     ╱ (5)
                              3     ╱   ← сигнал: закрылась свеча, пробившая V(3)
                             ╱╲    ╱      вход SELL по close
                        1   ╱  ╲  ╱
                       ╱╲  ╱    ╲╱ 4  ← конец W4 в зоне W1 (перекрытие, hard)
                      ╱  ╲╱ 2
                     ╱   (W2 не ниже точки 0)
                    0                     ┄┄┄┄┄┄┄ TP = entry − R·(SL − entry)
```

- **0** — начало волны 1 (и всей диагонали); для `isUp` — low.
- **1, 3, 5** — моторные волны по направлению диагонали; **2, 4** — коррекции.
- Клин **сжимается**: `|W3| < |W1|`, `|W4| < |W2|`, `|W5| < |W3|`.
- **Перекрытие**: конец W4 заходит в ценовую зону W1 (ниже V(1) для `isUp`) —
  главный признак, отличающий диагональ от импульса.

Соглашение о направлении: `isUp` описывает **диагональ**. Сделка всегда
противоположна: бычья диагональ → SELL (TP ниже входа), медвежья → BUY.

---

## 4. Инварианты диагонали (hard rules)

Для `isUp` (медвежий случай — зеркально). Все проверки через знак
`s = isUp ? 1 : −1`, один код на оба направления.

- **D-W2** — `s·(V(2) − V(0)) > 0`: волна 2 не уходит за начало волны 1.
- **D-W3-PEN** — `s·(V(3) − V(1)) ≥ MIN_DIAGONAL_PENETRATION·|W1|`: волна 3
  делает новый экстремум за волной 1 (минимальный пробой 5% |W1|, как в v2).
- **D-CONTRACT-3** — `|W3| < |W1|` (сжатие).
- **D-CONTRACT-4** — `|W4| < |W2|` (сжатие).
- **D-W4-78** *(опционально, флаг `RequireWave4Ratio`)* — `|W4| ≥ 0.786·|W2|`:
  вместе с D-CONTRACT-4 даёт коридор `0.786·|W2| ≤ |W4| < |W2|` — клин
  сжимается равномерно, а не схлопывается крошечной волной 4. По
  умолчанию **выключено**: на архиве фильтр режет выборку втрое и
  ухудшает матожидание (§9.4).
- **D-OVERLAP** — `s·(V(4) − V(1)) < 0`: конец волны 4 **в ценовой зоне
  волны 1** (перекрытие; hard — решение O-2). Без перекрытия это импульс,
  а не диагональ.
- **D-W4-2** — `s·(V(4) − V(2)) > 0`: волна 4 не пробивает конец волны 2
  (следствие сжатия, но проверяем явно — экстремумы могут разъехаться).
- **D-W5-BREAK** — волна 5 пробивает конец волны 3: `s·(H5 − V(3)) > 0`,
  где `H5` — бегущий экстремум волны 5 по сырым барам. Усечения исключены;
  это же — **триггер сигнала** (§6).
- **D-W5-CAP** — `|W5| < |W3|`: бегущая длина волны 5 меньше длины волны 3.
  Достижение 100% = инвалидация модели = уровень стопа (§6).
- **D-W5-78** *(опционально, флаг)* — на момент сигнала `|W5| ≥ 0.786·|W3|`:
  волна 5 уже «взрослая», вход ближе к теоретическому потолку → лучше R:R
  фактического разворота (§6.1).
- **D-CONTAIN** — внутри каждой из волн 1–4 цена не выходит за её концы
  (interior-бары, инвариант I2, как `IsWaveContained` в треугольниках).
- **D-TIME** — sanity длительностей соседних волн:
  `bars(Wk)/bars(Wk−1) ≤ MAX_WAVE_DURATION_RATIO` (ловит волны, «сшитые»
  через разрывы, как в треугольниках).
- **D-W1-INIT** *(опционально, флаг `RequireInitialMovement`)* — движение
  `V(0) → V(1)` проходит тест `IsInitialMovement`: перед точкой 0 нет бара,
  который зашёл бы дальше `V(1)`, до того как цена вернётся за `V(0)`. То есть
  диагональ стартует «со свежего разворота». Это **контекстный**, а не
  типовой фильтр (§5.2): он не разделяет начальные и конечные диагонали.
  По умолчанию **выключено**; статистика — §9.4. Проверяется **последним** из
  гейтов: это единственное правило, которое отматывает бары назад.

Soft-фильтры — **выключены на первом этапе** (решение O-7):

- фибо-соотношение `W3/W1` ∈ 0.5–0.786 (`CONTRACTING_DIAGONAL_3_TO_1`,
  EW_RULES.md §4.5). Сначала собираем статистику без него (§9.1) и только потом
  решаем, включать ли. В коде оставляем точку расширения (константы
  `W3_TO_W1_MIN/MAX` + флаг), чтобы включение было однострочным;
- сходимость трендлиний 1-3 и 2-4 (следует из сжатия, отдельно не проверяем).

---

## 5. Откуда считать волну 1 (проблема точки 0)

### 5.1. Зачем вообще нужна точка 0

Триггер сетапа — события волн 4–5, а точка 0 нужна **только для того, чтобы
измерить волну 1** и тем самым проверить **сомасштабность волн 1 и 3**
(решение O-6). От `V(0)` зависят ровно два правила:

- **D-W2** — волна 2 не уходит за начало волны 1;
- **D-CONTRACT-3** — `|W3| < |W1|` (и, в перспективе, soft-фибо `W3/W1`, §4).

Всё остальное (перекрытие W4↔W1, сжатие `|W4|<|W2|`, пробой и потолок волны 5,
стоп и тейк) считается **без точки 0**. Поэтому ошибка в точке 0 не разрушает
сетап целиком — она лишь ослабляет проверку сомасштабности.

### 5.2. Кандидаты в точку 0 — пивоты своего рунга, без «заходности»

1. **Кандидат — каждый подтверждённый пивот** нужного знака (v-low для `isUp`)
   на каждом рунге лестницы периодов (§8). Крупные диагонали резолвятся на
   крупном рунге, мелкие — на тонком; дубли схлопываются дедупликацией по
   точке 0.
2. Волна 1 собирается тем же жадным слиянием подволн, что и волны 2–4
   (§7.2), поэтому точка 0 — это просто пивот, с которого волна 1 стартует.
3. **Тест `IsInitialMovement` по умолчанию выключен** (решение O-6). Он требует,
   чтобы перед
   точкой 0 было контр-движение высотой ≥ |W1|, то есть чтобы диагональ
   начиналась «со свежего разворота». Для **начальной** диагонали (волна 1/A
   нового движения, стартует прямо из конца предыдущего тренда) это далеко
   не всегда верно. Мы ищем оба типа (O-5), поэтому гейт не может быть
   обязательным — тот же урок, что и с волной A треугольников
   (EW_R_TRIANGLE.md §2: этот гейт убивал ~80% кандидатов).
4. **Чего гейт НЕ делает** — он не разделяет начальные и конечные диагонали.
   «Свежий разворот» перед точкой 0 бывает у конечной диагонали в волне 5
   импульса, но точно так же — у конечной диагонали в **волне C плоскости**
   (волна B — готовое контр-движение перед точкой 0), в волне C зигзага и
   у части начальных диагоналей после глубокой коррекции. Гейт отбирает
   только контекст старта (чистый разворот против «продолжения с хода»),
   а последующая цель у этих случаев разная (после волны C плоскости идёт
   продолжение старшего тренда, а не обычная коррекция), поэтому выборка
   под флагом остаётся разнородной — что и видно по статистике §9.4.

> Отбор «мусорных» точек 0 обеспечивают сами структурные правила §4:
> случайно выбранная точка 0 почти всегда ломает либо D-W2, либо
> D-CONTRACT-3.

### 5.3. Cross-scale сборка

Как и у running-треугольника (EW_R_TRIANGLE.md §7.6), скелет и триггер живут
на разных масштабах:

- **точка 0 и волны 1–4** — пивоты рунга, на котором они чисто резолвятся
  (перебор по лестнице, коарсест-фёрст); точка 0 нужна только для измерения
  волны 1 (§5.1);
- **волна 4 может быть «плавающей»** — её конец двигается на новый экстремум,
  пока не начался пробой (аналог floating C у треугольника);
- **волна 5 целиком по сырым барам**: от бара конца W4 трекаем бегущий экстремум
  `H5`; события «пробит V(3)», «достигнуто 0.786·|W3|», «достигнуто |W3|»
  проверяются на каждой закрытой свече, без ожидания пивота зигзага.

---

## 6. Сигнал (entry / TP / SL)

Все решения по механике подтверждены (§11). Для `isUp` (бычья диагональ → SELL):

- **Триггер**: закрылась свеча, чей high пробил `V(3)` (пробой хвостом
  достаточен, закрытие за уровнем не требуется — решение O-3), при этом волны
  1–4 собраны и ни одно правило §4 не нарушено.
- **Вход**: market по цене закрытия триггерной свечи, **против** диагонали.
- **Stop-Loss**: `SL = V(4) + |W3|` (для `isUp`; зеркально `V(4) − |W3|`) —
  теоретический потолок волны 5 в сжимающейся диагонали. Плюс
  `PERCENT_ALLOWANCE_SL`, округление до `Symbol.Digits`. Пробой SL =
  одновременно инвалидация модели (D-W5-CAP), лишних «сирот»-стопов нет.
- **Take-Profit**: зависит от режима `TakeProfitMode` (§6.3). По умолчанию
  (`RISK_RATIO`) — `TP = entry − R·(SL − entry)`, где `R = TakeProfitRatio`
  (параметр, по умолчанию **1.0** — решение O-4; `R = 2` даст цель вдвое
  дальше стопа). TP считается от **уже округлённого** SL, поэтому
  фактический R:R точно равен заданному (отдельного `PERCENT_ALLOWANCE_TP`
  нет — он ломал бы ровное соотношение).
- **Отбраковка**: сигнал не выдаётся, если на триггерной свече TP или SL уже
  задет, либо риск вырожденно мал:
  `|SL − entry| < MIN_RISK_TO_W3_RATIO·|W3|`. Последнее важно: чем ближе волна 5
  к потолку `|W3|`, тем тоньше стоп — вырожденный стоп даёт фантастический
  R:R на бумаге и мгновенный вынос на практике.
- **Дедупликация — по точке 0** + TP/SL-подпись (`SignalKey`): одна диагональ =
  один сигнал, дубли между рунгами лестницы схлопываются.

### 6.1. Опция «зрелая волна 5» (`RequireWave5Ratio`)

Флаг (по умолчанию **выключен**). Если включён, сигнал выдаётся только когда
**оба** условия выполнены на закрытой свече:

1. пробой `V(3)` (обязателен всегда);
2. `|W5| ≥ WAVE5_MIN_RATIO·|W3|` (константа 0.786).

Если пробой V(3) случился раньше отметки 78.6% (глубокая волна 4), кандидат
ждёт в фазе `ARMED_WAIT` — сигнал сработает на той свече, где длина волны 5
дотянется до 78.6%. Если раньше этого волна 5 достигнет 100% |W3| — кандидат
инвалидируется (D-W5-CAP), сигнала не будет.

> Смысл опции: вход ближе к теоретическому потолку волны 5 → короче путь до
> стопа при том же стопе → фактический R:R разворота лучше. Плата — часть
> диагоналей развернётся, не дотянув до 78.6%, и сигнал будет пропущен.
> Сравнение обоих режимов — задача самотеста (§9).

### 6.2. Пост-сигнальное сопровождение

После входа сопровождение стандартное (TP/SL-хиты по барам, как во всех
финдерах). Отдельной пост-сигнальной инвалидации, как у треугольника
(`invalidatedE`), не требуется: единственный сценарий «модель умерла» —
волна 5 длиннее волны 3 — совпадает с уровнем SL по построению.

### 6.3. Режимы тейк-профита (`TakeProfitMode`)

Два варианта цели; выбор — параметр финдера.

- **`RISK_RATIO`** *(по умолчанию)* — фиксированный R:R от стопа (§6).
  Цель не привязана к геометрии движения, зато статистика читается напрямую.
- **`DIAGONAL_RETRACE`** — **D-TP-236**: цель на уровне коррекции **23.6% всей
  диагонали**: `TP = W5 ∓ 0.236·|W5 − V(0)|` (знак — против диагонали), где
  `W5` — бегущий экстремум волны 5 на триггерной свече. `TakeProfitRatio`
  игнорируется, **R:R становится переменным** и определяется геометрией
  клина: чем выше диагональ относительно остатка волны 5 до потолка `|W3|`,
  тем больше R. Логика: минимальная коррекция после диагонали — это
  процент от самого движения, а не от того, где случайно встал стоп.
  Дополнительный гейт `tpBehindEntry`: если триггерная свеча закрылась уже
  за уровнем 23.6% (длинный откат внутри свечи), сигнал не выдаётся.

---

## 7. Инкрементальный алгоритм (жизненный цикл кандидата)

Архитектура повторяет `RunningTriangleSetupFinder`: **stateful**, кандидаты
живут между барами. Отличие одно, но принципиальное: триггер — событие
**бара**, а не пивота, поэтому работа разделена на два контура с разной
частотой:

| Контур | Когда | Стоимость |
|---|---|---|
| Сборка скелета 0-1-2-3-4 по пивотам | только когда список пивотов рунга **изменился** (вырос `Count` либо сдвинулся последний экстремум) | дорого, но редко |
| Ведение волны 5 по сырым барам | каждый бар, для всех живых кандидатов | O(1) на кандидата |

### 7.1. Состояние кандидата

```
DiagonalCandidate {
    BarPoint Point0, P1, P2, P3, P4;  // скелет, уже прошедший §4 (правила без W5)
    bool     IsUp;                    // направление ДИАГОНАЛИ (торгуем против)
    double   W5Extreme;               // бегущий экстремум волны 5 (сырые бары)
    int      W5ExtremeBar;            // бар этого экстремума
    bool     WasTriggerable;          // выполнялись ли условия сигнала ДО текущего бара
}
```

Ключ кандидата — `(Point0.OpenTime, P4.OpenTime, IsUp)`: повторная сборка того же
скелета (в т.ч. на соседнем рунге лестницы) не создаёт дубликата и не
сбрасывает уже набранную волну 5. Флаг `WasTriggerable` гарантирует сигнал
ровно на **первой** свече, где условия выполнились.

### 7.2. Порождение (по изменению пивотов рунга)

Конец волны 4 — это **последний** пивот рунга (пока волна 5 ещё не
зарегистрирована как экстремум) либо предпоследний (когда волна 5 уже
отклонилась на `deviation%` и породила свой плавающий экстремум). Оба
варианта проверяются. Для каждого из них:

1. направление `isUp` выводится из знака пивота (конец W4 бычьей
   диагонали — локальный минимум);
2. перебор кандидатов в точку 0 вглубь на `MAX_ASSEMBLY_DEPTH` пивотов;
3. волны 1–4 собираются жадным слиянием подволн (`ExtendWave`, тот же
   алгоритм, что у треугольников); конец волны 4 обязан совпасть со выбранным
   пивотом;
4. прогоняются все правила §4, кроме W5-шных → кандидат в пул.

### 7.3. Ведение волны 5 (каждый бар, сырые бары)

Для каждого кандидата пула на закрытой свече:

1. если цена ушла за `V(4)` против направления диагонали → `DEAD` (конец W4
   ещё не сформирован; новый кандидат с более глубоким P4 родится сам);
2. обновить `W5Extreme` по high/low текущего бара;
3. `|W5| ≥ |W3|` → `DEAD` (D-W5-CAP);
4. длительность волны 5 нарушила D-TIME → `DEAD`;
5. условие сигнала = пробой `V(3)` **и** (если `RequireWave5Ratio`)
   `|W5| ≥ 0.786·|W3|`. Сигнал выдаётся, когда условие выполнилось сейчас и
   не выполнялось до этой свечи (`WasTriggerable == false`).

### 7.4. `CheckSetup(openDateTime)` — план

1. Отработать активный сетап (TP/SL-хиты).
2. Обновить лестницу зигзагов; на рунгах с изменившимся списком пивотов —
   прогнать §7.2.
3. Прогнать §7.3 по всему пулу, убрать `DEAD`.
4. Первый сработавший кандидат → `OnEnterInvoke` с `ElliottWaveSignalEventArgs`
   (WavePoints = [0,1,2,3,4,5]).

---

## 8. Константы и параметры

| Имя | Назначение | Стартовое значение |
|---|---|---|
| `Period` (EWParams) | Базовый период зигзага; 0 → авто. | 0 (авто) |
| `TakeProfitRatio` | Множитель TP от дистанции до стопа (R:R). | **1.0** |
| `TakeProfitMode` | `RISK_RATIO` или `DIAGONAL_RETRACE` (D-TP-236, §6.3). | `RISK_RATIO` |
| `DIAGONAL_RETRACE_RATIO` | Доля коррекции диагонали для TP. | 0.236 |
| `RequireWave5Ratio` | Требовать `|W5| ≥ 0.786·|W3|` на сигнале (§6.1). | `false` |
| `WAVE5_MIN_RATIO` | Порог зрелости волны 5. | 0.786 |
| `RequireWave4Ratio` | Требовать `|W4| ≥ 0.786·|W2|` (D-W4-78, §4). | `false` |
| `WAVE4_MIN_RATIO` | Нижняя граница `|W4|/|W2|`. | 0.786 |
| `RequireInitialMovement` | Требовать «заходности» волны 1 (D-W1-INIT, §5.2). | `false` |
| `MIN_DIAGONAL_PENETRATION` | Мин. пробой W3 за W1 (доля |W1|). | 0.05 (как в v2) |
| `MIN_RISK_TO_W3_RATIO` | Мин. риск (SL−entry) как доля \|W3\| — отсев вырожденных стопов. | 0.05 |
| `MAX_WAVE_DURATION_RATIO` | Sanity длительностей соседних волн. | 4.0 |
| `MAX_ASSEMBLY_DEPTH` | Глубина перебора кандидатов в точку 0 (пивоты). | 40 |
| `LADDER_RATIOS` | Лестница периодов. | как в `RunningTriangleSetupFinder` |
| `PERCENT_ALLOWANCE_SL` | Аллоуанс SL. | `Helper.*` (как сейчас) |

---

## 9. Самотест по архивным CSV

Два контура проверки — автоматический (NUnit) и визуальный (ReplayViewer),
по образцу треугольников.

### 9.1. NUnit-тесты (`TradeKit.Tests/DiagonalSetupFinderTests.cs`)

Данные — репозиторные [data/*.csv](data/) (поиск каталога через `FindDataDir()`:
подъём от `TestContext.CurrentContext.TestDirectory` до папки с `data/` +
`TradeKit.sln`). Финдер прогоняется по-барно (causal), как в
`RunningTriangleSetupFinderTests`.

**Инварианты каждого выданного сигнала** (assert в цикле по `OnEnter`):

- волны упорядочены по времени, направления чередуются;
- сжатие: `|W3| < |W1|`, `|W4| < |W2|`;
- перекрытие: конец W4 в зоне W1 (D-OVERLAP);
- пробой: на триггерном баре экстремум за `V(3)` (D-W5-BREAK);
- `SL == V(4) ± |W3|` (с точностью до аллоуанса/округления);
- `TP` соответствует `TakeProfitRatio`, а в режиме `DIAGONAL_RETRACE` — уровню
  `W5 ∓ 0.236·|W5 − V(0)|`;
- сделка против диагонали (бычья диагональ → TP ниже входа);
- при `RequireWave5Ratio` — `|W5| ≥ 0.786·|W3|` на триггерном баре;
- при `RequireWave4Ratio` — `|W4| ≥ 0.786·|W2|`;
- при `RequireInitialMovement` — сигналы остаются валидными и их не больше,
  чем без флага (гейт только сужает выборку).

**Funnel-диагностика**: research-тест `[Explicit, Category("Research")]` (по
образцу `PeriodSweepTests.DiagnoseTriangleRejections`) гоняет финдер по
нескольким файлам (m15 + h1) и пишет `reports/diagonal_rejections.md` —
счётчики `Diag` по гейтам (`w2Breach`, `noContract3`, `noOverlap`,
`w5Cap`, `tooCloseToSl`, `entered`, …). Это главный инструмент поиска
«гейта-убийцы» до какой-либо ручной калибровки (урок треугольников: один
неуместный гейт убивал 80% кандидатов).

**Сравнение режимов**: тот же research-тест прогоняет все комбинации
`RequireWave5Ratio` × `RequireWave4Ratio` × `RequireInitialMovement` (false/true) и
цели (`TakeProfitRatio` 1.0 / 1.5 / 2.0 / 3.0 плюс отдельный прогон
`DIAGONAL_RETRACE`) и пишет сводку TP/SL-исходов — эмпирический выбор
дефолтов. Матожидание считается по **фактическому R каждой сделки**
(в retrace-режиме он плавающий), в отчёт выводится средний `avgR`.

**Референсный пример** — по образцу EW_R_TRIANGLE.md §12: после первых прогонов
выбираем на архивном CSV реальную диагональ, фиксируем якоря (точка 0, точка 5,
направление, триггерная свеча) и закрепляем тестом. Кандидатов ищем визуально
через страницу §9.2. *(Раздел заполняется после первой реализации.)*

### 9.2. Страница самопроверки в ReplayViewer (quiz) — реализовано

По образцу running-треугольника (`rtriangle.html`):

- **[DiagonalScanEngine.cs](TradeKit.ReplayViewer/Services/DiagonalScanEngine.cs)** —
  клон `RunningTriangleScanEngine`: резолв символа/таймфрейма из имени CSV,
  `EWParams` из инпутов страницы, по-барный прогон финдера, сбор
  `OnEnter` → `OnTakeProfit`/`OnStopLoss` в DTO с исходом (`TP`/`SL`),
  срез свечей под отрисовку. DTO переиспользованы без изменений
  (`TriangleScanResult` / `TriangleSetupDto`, 6 wave-точек 0–5); запрос —
  `DiagonalScanRequest { File, FromDate, ToDate, Period, MinSizePercent,
  BarsCount, TakeProfitRatio, RequireWave5Ratio, RequireWave4Ratio,
  RequireInitialMovement, TakeProfitAtRetrace }`.
- **`POST /api/diagonal/scan`** в [Program.cs](TradeKit.ReplayViewer/Program.cs) —
  по образцу `/api/rtriangle/scan` (имя файла нормализуется
  `Path.GetFileName`, чтобы запрос не вышел за `dataDir`).
- **[diagonal.html](TradeKit.ReplayViewer/wwwroot/diagonal.html) +
  [diagonal.js](TradeKit.ReplayViewer/wwwroot/js/diagonal.js)** — quiz-UI:
  зигзаг 0-1-2-3-4-5, трендлинии 1-3 и 2-4 (сходящийся клин), уровни
  W3/entry/TP/SL, маркеры волн; пользователь угадывает исход (TP/SL),
  статистика ответов. Параметры: Период (0=авто), Размер %, Баров,
  R:R, чекбоксы «W5 ≥ 78%», «W4 ≥ 78%», «Заходность W1» и «TP 23.6%».
- Ссылка «◺ Диагонали» добавлена в навигацию index/impulse/triangle/rtriangle.

### 9.3. Критерии приёмки самотеста

- На полном прогоне нескольких пар (m15 и h1) финдер выдаёт **ненулевое, но
  осмысленное** число сетапов (ориентир по треугольникам: единицы–десятки на
  файл-историю, не сотни).
- Ни один выданный сигнал не нарушает инварианты §9.1.
- Визуальная проверка на quiz-странице: клин сжимается, перекрытие W4↔W1
  видно глазом, триггерная свеча пробивает V(3).
- Funnel-отчёт не показывает гейта, убивающего >70–80% кандидатов без
  структурной причины.

### 9.4. Фактические результаты первого прогона

Полный отчёт — [reports/diagonal_rejections.md](reports/diagonal_rejections.md).
По AUDUSD (полная история), `TakeProfitRatio = 1.5`, soft-фильтры выключены:

| файл | период зигзага | скелетов собрано | кандидатов в пуле | сигналов |
|---|---:|---:|---:|---:|
| `AUDUSD_m15` | 26 | 530 004 | 4 238 | **496** |
| `AUDUSD_h1` | 47 | 160 260 | 1 117 | **135** |

Гейта-убийцы нет: основной отсев приходится на чисто геометрические
правила (`w2BeyondStart` → `w3NoPenetration` → `w3NotContracting` →
`w4NotContracting`), что ожидаемо: перебор точек 0 порождает много заведомо
невалидных скелетов. Среди зарегистрированных кандидатов отсев распределён
ровно (`durationInsane` ≈ 44%, `tooFewBars` ≈ 35%, `noOverlap` ≈ 31%,
`notContained` ≈ 7%), время полного прогона — единицы секунд.

Сравнение режимов (`Diagonal_ModeComparison_Report`, `AUDUSD_h1`):

| `RequireWave5Ratio` | R:R | входов | win% | матожидание |
|---|---:|---:|---:|---:|
| false | 1.0 | 135 | 51.9% | +0.04R |
| false | 1.5 | 144 | 41.0% | +0.02R |
| false | 2.0 | 139 | 33.1% | −0.01R |
| false | 3.0 | 127 | 21.3% | −0.15R |
| true | 1.0 | 58 | 43.1% | −0.14R |
| true | 1.5 | 72 | 41.7% | +0.04R |
| true | **2.0** | 78 | 35.9% | **+0.08R** |
| true | 3.0 | 86 | 25.6% | +0.02R |

Выводы: геометрия работает (win% стабильно выше безыскового уровня
`1/(1+R)` на малых R), но края тонкие — без фильтра контекста это не готовая
торговая система, а база для тренажёра и дальнейшей фильтрации.
«Зрелая волна 5» окупается только на R ≥ 1.5.

Добавленный позже фильтр `RequireWave4Ratio` (D-W4-78) на той же выборке
ухудшает результат во всех комбинациях:

| `RequireWave5Ratio` | `RequireWave4Ratio` | R:R | входов | win% | матожидание |
|---|---|---:|---:|---:|---:|
| false | true | 1.0 | 41 | 41.5% | −0.17R |
| false | true | 1.5 | 55 | 34.5% | −0.14R |
| false | true | 2.0 | 59 | 25.4% | −0.24R |
| false | true | 3.0 | 61 | 13.1% | −0.48R |
| true | true | 1.0 | 23 | 30.4% | −0.39R |
| true | true | 1.5 | 35 | 37.1% | −0.07R |
| true | true | 2.0 | 39 | 30.8% | −0.08R |
| true | true | 3.0 | 43 | 18.6% | −0.26R |

Выборка режется в 2–3 раза, а win% падает сильнее, чем растёт качество:
на архиве ровное сжатие клина не коррелирует с качеством разворота.
Поэтому флаг остаётся выключенным по умолчанию — он полезен как
инструмент отбора «красивых» примеров для тренажёра, но не как торговый
фильтр. Проверять на других парах до окончательного вывода.

Фильтр `RequireInitialMovement` (D-W1-INIT) — фактически срез «только
конечные диагонали» — на том же архиве оставляет ~43% входов и почти не
меняет края:

| `RequireWave5Ratio` | `RequireWave4Ratio` | R:R | входов | win% | матожидание |
|---|---|---:|---:|---:|---:|
| false | false | 1.0 | 58 | 53.4% | **+0.07R** |
| false | false | 1.5 | 64 | 40.6% | +0.02R |
| false | false | 2.0 | 65 | 29.2% | −0.12R |
| false | false | 3.0 | 64 | 18.8% | −0.25R |
| true | false | 1.0 | 30 | 46.7% | −0.07R |
| true | false | 1.5 | 38 | 44.7% | **+0.12R** |
| true | false | 2.0 | 40 | 32.5% | −0.03R |
| true | false | 3.0 | 43 | 20.9% | −0.16R |
| false | true | 1.0 | 19 | 52.6% | +0.05R |
| true | true | 1.5 | 17 | 41.2% | +0.03R |

На R:R = 1.0 win% чуть выше (53.4% против 51.9%), в связке с «зрелой
волной 5» на R:R = 1.5 даёт лучшее матожидание по всей сетке (+0.12R), но
выборка в 38 входов слишком мала, чтобы считать это подтверждением.
Вывод: «заходность» — не улучшение краёв и **не** классификатор типа
диагонали (её проходит и конечная диагональ в волне C плоскости, и часть
начальных, §5.2 п. 4), а фильтр **контекста старта**. Дефолт — выключено;
полезен для тренажёра, чтобы отбирать примеры с чистым разворотом перед
точкой 0.

### 9.5. Режим TP «23.6% диагонали» (D-TP-236)

`TakeProfitMode = DIAGONAL_RETRACE`, тот же AUDUSD h1. R:R здесь плавающий,
поэтому матожидание считается по фактическому R каждой сделки; `avgR` —
средний R на входе:

| `RequireWave5Ratio` | `RequireWave4Ratio` | `RequireInitialMovement` | входов | avgR | win% | матожидание |
|---|---|---|---:|---:|---:|---:|
| false | false | false | 100 | 0.62 | **64.0%** | **+0.01R** |
| true | false | false | 58 | 1.72 | 39.7% | −0.02R |
| false | true | false | 30 | 0.96 | 46.7% | −0.14R |
| false | false | true | 41 | 0.70 | 58.5% | −0.06R |
| false | true | true | 15 | 1.11 | 53.3% | +0.01R |
| true | true | true | 11 | 1.52 | 27.3% | −0.30R |

Наблюдения:

- Цель, привязанная к геометрии, даёт **сильно более высокий win rate**
  (64% против 51.9% при фиксированном R:R = 1.0), но и `avgR` всего 0.62 —
  итог по матожиданию тот же околонулевой (+0.01R). Это ровно тот же край,
  просто переупакованный в другую пару «частота × размер».
- Опция «зрелая волна 5» и режим 23.6% **работают в одну сторону**: чем
  ближе вход к потолку `|W3|`, тем короче стоп и тем дальше 23.6%-цель →
  `avgR` растёт с 0.62 до 1.72, а win rate падает до 39.7%. Пара
  «W5 ≥ 78% + TP 23.6%» — самый «естественный» режим по построению, но на
  архиве он не лучше фиксированного R:R.
- Входов меньше (100 против 135): часть сигналов отсекается новым гейтом
  `tpBehindEntry` — триггерная свеча закрылась уже за уровнем 23.6%.

---

## 10. Перспектива: cbot и индикатор

После стабилизации финдера — стандартная обвязка (по аналогии с
`RunningTriangleFinderBaseIndicator` и [HarmonicPatternFinder](TradeKit.Core/Harmonic/HarmonicPatternFinder.cs) →
`HarmonicSignalerBot`):

- `TradeKit.cTrader/…/DiagonalFinderBaseIndicator.cs` — параметры
  `TakeProfitRatio`, `RequireWave5Ratio`, `RequireWave4Ratio`,
  `RequireInitialMovement`, `TakeProfitMode`, `Period` (0=авто);
  **не забыть** `<Compile Include>` в csproj (`EnableDefaultItems=False`);
- сигнальный бот по образцу существующих `*SignalerBot`;
- ядро (`DiagonalSetupFinder`) остаётся в `TradeKit.Core/ElliottWave/` и
  ничего не знает о cTrader — как все финдеры.

---

## 11. Решённые вопросы

- **O-1. Только сжимающиеся диагонали** (`DIAGONAL_CONTRACTING_*`).
  Expanding исключены: редки, стоп от |W3| для них не работает.
- **O-2. Перекрытие W4↔W1 — hard-правило** (D-OVERLAP). Без перекрытия
  кандидат — импульс, а не диагональ.
- **O-3. Вход**: market на закрытии свечи, пробившей `V(3)` (пробой
  хвостом/хай-лоу достаточен), против направления диагонали.
  `SL = V(4) ± |W3|`, `TP = entry ∓ R·(SL − entry)`.
- **O-4. `TakeProfitRatio` по умолчанию = 1.0** (R:R = 1).
- **O-5. Initial/ending не различаем**: геометрия и торговая механика
  одинаковы, надёжной классификации на уровне зигзаг-пивотов нет. Тип можно
  выводить в комментарий сигнала как метку позже, если появится контекст
  старшей структуры.
- **O-6. `IsInitialMovement` — опция, а не обязательное правило.** Точка 0
  нужна **только** для измерения волны 1 и проверки сомасштабности волн 1 и 3
  (§5.1). Требовать «свежести» точки 0 по умолчанию нельзя — начальные
  диагонали стартуют прямо из конца предыдущего движения. Флаг
  `RequireInitialMovement` (D-W1-INIT) добавлен как **контекстный** фильтр
  (старт со свежего разворота), **не** как классификатор «начальная/конечная»:
  его проходит и конечная диагональ в волне 5 импульса, и конечная в волне C
  плоскости, и часть начальных (§5.2, п. 4).
- **O-7. Soft-фильтр фибо `W3/W1` остаётся выключенным** (§4). Funnel §9.4
  показал, что гейта-убийцы нет и без него; включать только если появится
  замеренный выигрыш по матожиданию.
- **O-8. Референсный пример** (§9.1) подбираем после визуального просмотра
  quiz-страницы; кандидатов предлагает сам финдер (89 сетапов на AUDUSD h1
  за 2022 год при R:R = 1.5).
- **O-9. Режим TP «23.6% диагонали» — опция, дефолт `RISK_RATIO`.**
  Геометрическая цель осмысленнее фиксированного R:R (минимальная коррекция
  измеряется от самого движения), но на архиве она лишь переупаковывает тот
  же край: win rate 64% при `avgR` 0.62 против 51.9% при R = 1.0, матожидание
  в обоих случаях ≈ 0 (§9.5). Оставляем выбор пользователю.
